# /etc/mara/gmail.env — владелец и группа те же, под кем ходит юнит; 600.
# В репо только этот пример.
# Клиент OAuth типа Desktop app: console.cloud.google.com → Credentials.
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
# contextd.py --pair gmail
MARA_CONTEXT_TOKEN=
