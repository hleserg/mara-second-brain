# Импортёр экспорта WhatsApp (scripts/whatsapp_import.py). Живёт в
# /etc/mara/whatsapp.env с правами 600, в репозиторий и вольт не попадает.
# Токен — своё устройство: на doctor `contextd --pair whatsapp-import`.
MARA_CONTEXT_TOKEN=
