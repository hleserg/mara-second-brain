# /etc/mara/tdlib.env — владелец sergey, права 600. Значения не коммитить.
# my.telegram.org → API development tools → создать приложение
TDLIB_API_ID=
TDLIB_API_HASH=
# ssh doctor 'python3 ~/mara-second-brain/scripts/contextd.py --pair tdlib' — токен показывается один раз
MARA_CONTEXT_TOKEN=
