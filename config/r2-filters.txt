# Исключения синка R2 (ТЗ §2.1). Порядок важен: rclone берёт первое совпадение.
- /.git/**
- /.smart-env/**
- /.basic-memory/**
- /.obsidian/workspace*.json
- **/.DS_Store
# Сырьё не покидает doctor (§6): исключаем каталог целиком, а не по
# расширению. По *.jsonl уезжали бы и git-логи, и любой будущий формат.
- /raw/**
# производный индекс copilot, 85 МБ, гонять между устройствами незачем (§1)
- /.obsidian/copilot-index-*.json
# В data.json плагинов лежат живые API-ключи (Copilot: OpenAI/Anthropic/OpenRouter).
# Секреты в облако не уезжают (ТЗ §11). Настройки плагинов остаются локальными.
- /.obsidian/plugins/*/data.json
+ **
