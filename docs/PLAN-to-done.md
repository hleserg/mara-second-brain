# План: от сегодняшнего состояния до доделанной, развёрнутой и проверенной Мары

> Ссылка на этот файл стоит в `docs/TZ-master.md` §18. Это карта маршрута, а не
> пошаговый TDD-скрипт: пошаговые шаги пишутся в момент взятия задачи
> (в теле PR или в `docs/superpowers/plans/`), потому что шаги, написанные
> за две недели до задачи, к моменту исполнения врут.

**Дата составления:** 2026-09-11. **Дерево:** `main` @ `4abd91d`.
**Автор:** Claude Opus 5. **Источник требований:** `docs/TZ-master.md`
(1448 строк), `docs/current-state-audit.md` (599 строк), issue #1/#12/#28/#39/#59,
переписка с владельцем.

---

## 0. Контекст: почему план именно такой

Владелец попросил один документ, который ведёт от «сегодня» к
«доделано, развёрнуто, проверено», с отдельно собранными в мешки задачами,
где нужен он сам.

Разведка перед составлением плана дала один решающий факт:

**Код ушёл далеко вперёд развёртывания и потока данных.**

| Что мерил | Команда | Значение |
|---|---|---|
| Тестов на `main` | загрузчик unittest, `4abd91d` | 427; после #82 на `0ca51cb` — 436 |
| Самопроверок | `run-tests.sh` → `grep -c "^ok  "` | 29 |
| Отставание doctor | `git rev-list --count HEAD..origin/main` | 51 коммит |
| Событий в реестре | `select count(*) from events` | 2 |
| Звонков за 7 дней | тот же реестр | 1 |
| Заметок в волте | `find /srv/vault -name '*.md' \| wc -l` | 1667 |
| Записей в журнале решений | `grep -c '^## ' docs/decisions.md` | 86 |
| Файлов ADR | `ls docs/adr/` | 9 (из 12 обязательных) |
| Открытых issue / PR | `gh issue list` / `gh pr list` | 5 / 2 |

При этом `tdlib-ingest` на doctor **выключен**, у Gmail **нет OAuth-клиента**
(крон печатает «нет входа: нет GMAIL_CLIENT_ID»), телефон последний раз
присылал данные **2026-09-07**, `heartbeat`-файлов нет ни одного.

Отсюда форма плана: **сначала оживить поток данных (это почти целиком
мешок владельца), потом закрывать инженерию.** Писать Control Plane над
реестром, в котором два события, — строить панель над пустотой; §22.7 это
прямо запрещает («не строить Control Plane поверх нестабильной authority
model»), §22.8 запрещает новые коннекторы до закрытия рисков тихой потери.

**Два возможных финала, оба честные (§23):**

* **autonomous reliable memory capture** — если после перезагрузки телефона
  запись стартует сама и полевая матрица R01–R14 это подтверждает;
* **degraded prototype with monitored recovery** — если не стартует, но
  система это честно видит, кричит в измеренный срок и восстанавливается по
  воспроизводимой инструкции.

Второй финал — не провал и не подлежит маскировке под первый (§7, §17.2).
Какой из двух — решает мешок М4, не код.

---

## 1. Как читать и как вести этот план

1. **Два уровня.** Здесь — волны и карточки задач (цель / §ТЗ / файлы /
   критерий / размер / зависимости / кто). Пошаговые TDD-шаги пишутся в
   момент старта задачи. Не ищи их здесь и не считай их отсутствие пробелом.
2. **План самообновляемый.** PR, закрывающий пункт плана, **в том же PR**
   ставит галочку в этом файле. Issue #39 сгнил за шесть дней ровно потому,
   что этого правила не было. Закрытая волна — это ещё и обновлённая
   таблица §7 в `docs/current-state-audit.md` в том же PR (§22.14), а не
   только отметка в Т9.6.
3. **Размеры:** S ≈ один PR за присест, M ≈ один PR за день, L ≈ несколько
   PR подряд.
4. **«Кто»:** `агент` — делаю сам без вопросов; `владелец` — в мешке;
   `агент+гейт` — делаю сам, но выполняю только после явного разрешения.
5. **Числа — это утверждения.** Любое число в этом плане получено командой,
   и команда названа. Новое число не пишется без счёта (правило 16).
   Имя документа и номер строки — тоже утверждение про конкретное дерево;
   дерево называется (правило 20).

---

## 2. Требования, изменённые или добавленные в ходе разговора

Не всё это есть в ТЗ. Собрано из переписки, действует наравне с ТЗ.

| Когда | Требование | Следствие для плана |
|---|---|---|
| 2026-09-03 | Репозиторий **публичный** и приватным больше не станет; «убери оттуда всё чувствительное» | Ни имени владельца, ни рабочей почты, ни адресов домашней сети, ни названий личных папок, ни номеров телефонов — ни в коде, ни в ADR, ни в теле PR, ни в комментарии к issue |
| 2026-09-03 | Репозиторий публичный ⇒ **branch protection теперь доступна** (на приватном плане её не было) | Задача Т0.5, снимает первый пункт §17.6 |
| ранее | «мердж по готовности сам» | Мержу без спроса при выполненном §14.2а; выкат на doctor остаётся владельцу, пока он не передаст его явно |
| ранее | Codex недоступен ⇒ ревью делает субагент; **не больше 5 кругов** | Пятый круг без схождения = стоп и вопрос владельцу |
| ранее | «разгребай очередь PR», «жги, не останавливайся пока не упрёшься в блок» | Автономная работа по умолчанию; вопросы копятся в мешки |
| ранее | Дерево ACR — `[гггг]/[ММ]/[дд]/[номер телефона]`, приложение обязано его обходить | Сделано (`Дерево` в `Core.kt`, PR #76) |
| ранее | «Ключ подменил опять от роутера, не косячь» | Ключи не проходят через переменные bash, командную строку и heredoc; проверка косвенная (длина, HTTP 200) |
| ранее | «Рабочую почту не подключать. Вообще.» | Gmail-коннектор берёт только личный ящик; §12 |
| ранее | В Copilot на маке **никогда** не жать Index vault | Пункт мешка М1, повторяется в `USER-MANUAL-STEPS.md` §3 |
| ранее | Аудио и полные транскрипты не уходят во внешние LLM/embedding API | ASR только локально (bigpc/GTR); §7.3 |
| 2026-09-11 | «Составь план, зафиксируй в репе для себя и для будущих агентов, иди по нему» | Этот документ + Т0.2 |

---

## 3. Что вне этого репозитория (не задачи, а зависимости)

Названо, чтобы будущий агент не искал их в коде и не считал пропущенной работой.

* **`agent-os-*.service` на doctor** — три юнита от смежного проекта. Мара
  их не трогает; PR #85 (черновик, чужая ветка, английский) относится к ним.
* **Hermes-плагин `mara-context`** — версия 1.1.0 в `install/mara-context/`;
  его апстрим сильно впереди. Синхронизация — отдельное решение (мешок М5).
* **Порт 8770 на mac-mini** — занят личным процессом владельца, не нашим.
  Не принимать его за наш STT и не подключаться к нему.
* **STT на bigpc** — Windows, слушает 8770. Не наш код, но наша зависимость.
* **mc-server / GTR** — 16 ядер, 30 ГБ, **GPU нет**, крутит Minecraft.
  Тяжёлые задачи туда по умолчанию не идут (§13, §17.5).

---

## 4. Гейты и правила, действующие на весь план

**Г1. Гейт тестов.** `mkdir -p ~/.mara-tmp && TMPDIR=~/.mara-tmp
SKIP_ANDROID=1 bash scripts/run-tests.sh`. `mkdir -p` не украшение:
`run-tests.sh` делает песочницу через `mktemp -d "${TMPDIR:-/tmp}/…"`, а
`mktemp` не создаёт отсутствующий родительский каталог и падает с rc=1 до
первого теста — проверено на несуществующем `TMPDIR`. Число тестов ловится `grep -E "^Ran |^OK"`,
самопроверки — `rtk proxy grep -c "^ok  "`. `tail -N` обрезает строку `Ran` —
не пользоваться им для счёта.

**Г2. Гейт Kotlin.** Локального JDK нет. Проверка — либо job `kotlin` в CI,
либо `ssh doctor` + gradle. Kotlin-правка без одного из двух не считается
проверенной.

**Г3. Гейт мерджа (§14.2а).** CI зелёный + Г1 зелёный **на смерженном
дереве** + ревью пройдено + ≤5 кругов. Никогда не мержить на красном гейте,
при неразрешённом конфликте и при поломке живого потребителя `main`.

**Г4. Гейт опасной миграции (§14.1).** Миграция схемы реестра выполняется на
doctor только когда: doctor на `origin/main` **и** свежий `core-backup.py`
с пройденным учением восстановления **и** явное разрешение владельца.
Разработка — автономна, исполнение — за гейтом.

**Г4 не срабатывает сам, и один раз его уже прошли мимо.** Миграции реестра в
этом коде не отдельная команда: `_ужать_ledger()` зовётся из
`mara_ingest.connect()` и перестраивает `commitments`, `conversations`,
`projections` при каждом открытии базы. Значит выкат кода, который её
содержит, **и есть** миграция — а выглядит как обычный `reset --hard`. Выкат
2026-09-11 нёс `e55dfc8` (PR #79) и выполнил перестройку рестартом демона, без
разрешения и без учения восстановления. Урон нулевой и это проверено: во всех
трёх таблицах 0 строк, `*_old` не осталось. Защита теперь в шаге 2
`docs/RUNBOOK-deploy.md`: диапазон проверяется на `connect`/`_ужать_ledger`
до сброса дерева. Учение Т3б.1 из «хорошо бы» стало предусловием следующего
такого выката.

**Г5. Гейт выката — снят 2026-09-11.** Владелец передал выкат на doctor мне
(«разрешил», ответ на М5 п.1). Выкатываю сам: `main` на doctor,
`install-cron.sh --check` → `--apply`, рестарт `contextd`, оживление TDLib и
Gmail в той части, где не нужен его браузер. **Гейт Г4 это не отменяет**:
миграция схемы реестра (Т2.8) по-прежнему требует отдельного разрешения,
свежей копии и пройденного учения восстановления.

**Г6. Секреты.** Ключ не присваивается переменной bash, не идёт в
командную строку и не пишется в heredoc: любая ошибка печатает его целиком.
Читать и писать ключи — питоном на целевой машине, проверять косвенно.

**Г7. Публичность.** Перед каждым коммитом в публичный репозиторий —
проверка на домашние адреса, системные имена пользователей, имя владельца,
номера телефонов, названия личных папок.

---

## 5. Статус наблюдений аудита (перепроверено командами 2026-09-11)

| Пункт | Что | Проверка | Вердикт |
|---|---|---|---|
| P0-1 / N1 | `need_blob` на повторе закрывал работу без загрузки | `scripts/contextd.py:823` | **закрыт**: считается по таблице блобов |
| N5 | Повтор в другом месяце создаёт второй файл | `blob_path` + дедуп по sha в `blobs` | **снят дедупликацией**; предложенная правка «дата события в пути» не применена — косметика, не риск |
| N6 | Тело не вычитывается на 401/413 | `отлуп` в `scripts/contextd.py`: `слить(..., min(длина, СЛИВ))` до ответа, `СЛИВ = 8 << 20` | **закрыт** |
| N7 | Гонка в `put_event` | `scripts/mara_ingest.py:262` | **закрыт**: `IntegrityError` → повторный select → дубль |
| N11 | Недоставленный дайджест считался успехом | `scripts/call_digest.py:178` | **закрыт**: событие остаётся неотправленным |
| P0-3 | Реестр не является единственным авторитетом | нет ревизий, нет `expected_version` | **открыт** → волна 2 |
| P0-4 | Нет ревизий и конфликтов | там же | **открыт** → волна 2 |
| P0-7 | WhatsApp-дедуп может съесть два реальных события | `Messages.kt` | **открыт** → волна 3 |
| P0-8 | Evidence не валидируется по `segment_id` | нет модели evidence | **открыт** → волна 2/5 |
| P0-9 | Нет провайдера рекордера и монитора здоровья | нет `RecorderProvider` | **открыт** → волна 4 |
| P0-2, P0-5, P0-6, P0-10 | — | закрыты ранее | закрыты |

---

## 6. Волны

### Волна 0 — гигиена, разгрузка очереди (агент, сегодня)

- [x] **Т0.1 Смержить PR #82.** *(2026-09-11: гейт на смерженном дереве rc=0, `Ran 436 tests`, `OK (skipped=4)`, 29 самопроверок; смерджено как `0ca51cb`.)* Ревью Codex круг 3 чистое на `fa49858`,
  оба CI зелёные, `MERGEABLE/CLEAN`, пересечений файлов с двумя новыми
  коммитами `main` нет.
  *Шаги:* `git worktree remove ~/wt-39a` → Г1 на смерженном дереве
  (поймать `Ran N`, ожидание > 434) → `gh pr merge 82 --squash
  --delete-branch` → `git fetch && git merge --ff-only origin/main`.
  Размер S. Зависимостей нет.
- [x] **Т0.2 Этот план — в репозиторий.** `docs/PLAN-to-done.md` + строка-ссылка
  в `docs/TZ-master.md` §18. Отдельным PR, чтобы будущий агент нашёл его
  первым. Размер S. Зависит от Т0.1.
- [x] **Т0.3 Обновить память агента.** *(2026-09-11: сделано.)* `doctor-deploy-checklist.md`
  устарел (говорит `35a17aa`): реальность — doctor на
  `deploy/codex-usage-20260909` @ `54f8464`, 51 коммит позади, рестарта
  contextd требуют #47, #67, #71, #73, #82. Размер S.
- [x] **Т0.4 Судьба PR #85.** *(2026-09-11: владелец решил — не трогать.)*
  Черновик, английский, тема Agent OS — вне Мары. **Не трогать**: у ветки живое рабочее дерево
  `~/mara-usage-agent-os` @ `2464f7f`, рядом второе — `~/mara-codex-usage`.
  Это чужая работа в процессе, а не брошенная ветка; ни её, ни оба дерева
  не удалять. Решение — мешок М5 п.14. Размер S.
- [x] **Т0.5 Branch protection.** *(2026-09-11: включена мной через API, 403 не случилось; `required_status_checks: python, kotlin`, `allow_force_pushes: false`, `allow_deletions: false`, `enforce_admins: **true**` — с выключенным админ бы обходил обязательные проверки, и §17.6 п.1 закрывался бы ложно (замечание Codex, круг 4). Пункт мешка М1 снят. Работы CI при этом переименованы в `python` и `kotlin`: русское `name:` подменяло контекст, и PR застревал в BLOCKED навсегда.)* Теперь возможна: репозиторий публичный,
  защиты и рулсетов нет (`gh api repos/:owner/:repo/rulesets` пуст).
  Требуемые проверки: `python`, `kotlin`. Первый пункт §17.6.
  **Кто: агент** — пробую сам через `gh api -X PUT
  repos/{owner}/{repo}/branches/main/protection` (действие обратимое).
  Получу 403 — уходит в мешок М1 как запасной вариант. Размер S.
- [x] **Т0.6 Управление: CODEOWNERS, шаблон PR, правка CONTRIBUTING.** *(2026-09-11: `CODEOWNERS` и шаблон PR уже были на месте и годны; правка CONTRIBUTING — PR #87. Закрыты §17.6 п.2 и п.4.)*
  `CONTRIBUTING.md` до сих пор пишет, что мержит владелец — это разошлось с
  «мердж по готовности сам». Туда же — потолок в три субагента и бюджетные
  заставы (§17.6 п.4, §22.13) и правило «ревью Codex обязательно для PR по
  модели данных, миграциям, безопасности и рекордеру» (§17.6 п.3), с
  оговоркой про замену субагентом при недоступности Codex и ≤5 кругов.
  Размер S. §17.6.
- [ ] **Т0.8 Модель угроз и план миграций (§18 Фаза 0 п.3).** В
  репозитории их нет: ADR-0009 разбирает одну угрозу MCP, общей модели и
  плана миграций не существует. Минимально достаточная модель: границы
  доверия (телефон → contextd → реестр → волт → R2 → внешние LLM), что
  считаем злоумышленником, что — отказом, и какие данные не пересекают
  какую границу. План миграций — порядок, обратимость и точка отката для
  волны 2. Кладётся в `docs/threat-model.md` и `docs/migration-plan.md`.
  Критерий закрытия — не «модель написана», а покрыты все тринадцать
  сценариев §15.2: кража телефона, компрометация токена устройства,
  MITM и replay загрузки, вредоносный файл и неверный MIME, prompt
  injection в сообщении, письме и транскрипте, агент с избыточными правами
  MCP, утечка через логи, бэкапы и виджеты, повреждение и откат SQLite,
  сторонний вычислительный воркер, зависимость из supply chain, случайный
  публичный доступ к Control Plane, опасная массовая коррекция или
  миграция. Плюс требуемые §15.2 средства: опись секретов, ранбук ротации,
  сканирование зависимостей, минимальные права, аутентификация узлов,
  шифрование бэкапов, политика хранения и удаления. Размер M.
  **Блокирует Т2.1**: миграции без плана отката — ровно то, что §22.6
  запрещает.
- [ ] **Т0.7 Пересобрать отбор скилов** (`/skill-picker-revise`) — набор на
  диске изменился, блок устарел. 5 минут. Размер S.

### Волна 1 — оживить поток данных (владелец + агент, гейт Г5)

Без этой волны все следующие проверяются только на синтетике.

- [x] **Т1.1 Ранбук выката на doctor.** *(2026-09-11: `docs/RUNBOOK-deploy.md`, написан по фактическому прогону.)* Написать и положить в
  `docs/RUNBOOK-deploy.md`. Ключевой момент: у doctor два локальных коммита,
  и перед `reset --hard` надо **доказать их избыточность**:
  `git diff origin/main HEAD -- config/r2-filters.txt docs/codex-usage.md
  install/mara.cron scripts/codex-usage.py tests/test_codex_usage.py`
  должен быть пуст. Дальше: `install/install-cron.sh --check` → `--apply`
  → `systemctl restart contextd`. Размер S. Кто: агент.
- [x] **Т1.2 Выкат.** Кто: **агент** — полномочия переданы 2026-09-11.
  *(Прогон 2026-09-11: doctor с `54f8464` на ветке выката → `dca824f` на
  `main`; локальные коммиты доказаны избыточными, тег
  `deploy-before-20260911` оставлен точкой возврата; крон совпал, `--apply`
  не понадобился; `contextd` перезапущен, `/metrics` 200, импорт трёх
  модулей на 3.12.3 ок. Долг рестарта по #47, #67, #71, #73, #82 снят.)*
- [ ] **Т1.3 TDLib оживить.** `api_id`/`api_hash` с my.telegram.org в
  `/etc/mara/tdlib.env`, разовый `--login`, `systemctl enable --now
  tdlib-ingest`. Демон **никогда** не инициирует код входа сам.
  Кто: **владелец** (М1 + М2).
- [ ] **Т1.4 Gmail оживить.** OAuth Desktop client в Google Cloud,
  **обязательно Publish app** (иначе refresh-токен умирает через 7 дней),
  `ssh -L 8765:127.0.0.1:8765 doctor`, `gmail_ingest.py --login`, затем
  `--backfill --days 30`. Только личный ящик. Кто: **владелец** (М1 + М2).
- [ ] **Т1.5 Телефон.** Свежий APK, разрешения (включая доступ к
  уведомлениям и снятие оптимизации батареи), настройки ACR по §7.2,
  парный токен, «Проверка связи», отчёт мастера, контрольный звонок.
  Кто: **владелец** (М3).
- [ ] **Т1.6 Проверка, что поток пошёл.** Через сутки после М2+М3:
  события в реестре растут, `heartbeat` появились, источники не молчат.
  Кто: агент, автономно.

### Волна 2 — авторитет реестра (§17.1, Фаза 1) — агент, L

Самая большая инженерная волна. §22.7 держит Control Plane за ней.

- [ ] **Т2.0 Перенос канонического состояния из волта в реестр.** Самая
  опасная задача волны, и она идёт первой. Сегодня статус обязательства
  живёт только во frontmatter карточки, а правка владельца («сделано»,
  «отмени», «срок пятница») меняет строки на месте и дописывает журнал в
  тело; в базу попадает лишь факт события-правки, но не новый статус
  (`docs/current-state-audit.md` §2, наблюдение про `call_project.py`).
  Значит полная пересборка проекций из реестра — Т2.6 — **сотрёт все
  правки владельца**, а Т2.8 сменит авторитет на базу, в которой их нет.
  Нужен разбор существующих карточек в сущности и историю правок, сверка
  «сколько карточек — столько записей», и отчёт о том, что не разобралось,
  **до** любой смены авторитета. Размер L. **Блокирует Т2.6 и Т2.8.**
- [ ] **Т2.1 Схема и миграции.** Явная версия схемы (`schema_version`),
  `PRAGMA foreign_keys=on`, ограничения, обратимые миграции, тест на
  прогон миграции на копии боевой базы. §4, §5. Размер M.
  Зависит от Т0.8 (плана миграций и точки отката). Критерий закрытия —
  весь минимальный набор сущностей §4.2, а не пять существующих таблиц
  (`devices`, `events`, `jobs`, `blobs`, `digests`): `source_events`,
  `conversations`, `messages`, `transcripts` и `transcript_segments`,
  `entities` и `entity_aliases`, `commitments`, `decisions`,
  `facts`/`claims` с достоверностью и сроком годности, `evidence_refs`,
  `relations`, `revisions`, `corrections`, `projection_state`,
  `ingest_attempts`, `jobs` с `job_attempts` и DLQ, `audit_events`,
  `provider_health` и `alerts`, `compute_nodes`.
- [ ] **Т2.1б Эксплуатационные настройки и инварианты SQLite (§5.1).**
  Отдельно от схемы, потому что закрывается отдельными доказательствами:
  `busy_timeout`, явные транзакции, один контролируемый путь записи,
  prepared statements, UTC плюс сохранённый исходный сдвиг там, где он
  семантически важен, `integrity_check`/`quick_check` по расписанию,
  мониторинг размера WAL, свободного места, состязания за блокировку и
  состояния миграции, восстановление без опоры на чистое завершение, тесты
  на убитый процесс и повтор вокруг критических транзакций. Выбранные
  PRAGMA и режим синхронизации документируются в ADR-0005 по измерениям, а
  не по вкусу. §18 Фаза 2 п.2. Размер M.
- [ ] **Т2.1в Транзакционные границы (§5.2).** Шесть операций обязаны быть
  атомарными: событие источника плюс квитанция идемпотентности; объект
  плюс ревизия плюс событие аудита; переход обязательства плюс
  происхождение коррекции; успех работы плюс результат и запись outbox;
  приём метаданных блоба после fsync и rename; обновление контрольной
  точки проектора только после успешной записи проекций. Размер M.
  Связана с Т2.5 (audit и outbox) и Т2.9 (квитанции).
- [ ] **Т2.2 Стабильные ID в карточках.** Каждая каноническая сущность —
  неизменяемый ID, не зависящий от имени файла; ID проникает в Markdown
  как поле, а не как заголовок. ADR-0002 уже написан — привести код в
  соответствие. Размер M.
- [ ] **Т2.3 Ревизии, `expected_version`, конфликт 409.** Обновление без
  версии отклоняется; конфликт возвращается вызывающему и виден в UI/MCP.
  ADR-0003 написан. Размер L. **Ядро всей волны.**
- [ ] **Т2.4 Модель evidence.** `segment_id` + диапазон; ссылка,
  указывающая в несуществующий сегмент или за границы, не сохраняется.
  Закрывает P0-8. Размер M. Зависит от Т2.1.
- [ ] **Т2.5 Атомарные доменные команды, audit и outbox.** Одна запись —
  одна транзакция; след правки сохраняется; исходящие эффекты — через
  outbox, а не из середины транзакции. Размер M.
- [ ] **Т2.6 Проектор Markdown: полная пересборка и детектор расхождений.**
  Пересборка из реестра даёт детерминированный результат; расхождение
  находится проверкой, а не глазами. Размер M. **Зависит от Т2.0**: до
  переноса состояния пересборка не восстанавливает волт, а стирает правки
  владельца (§4.8 против сегодняшней модели авторитета).
- [ ] **Т2.7 Регрессии на коллизии.** Два одинаково названных
  обязательства; два звонка в одну минуту; повтор после потерянного
  ответа. §17.1. Размер S. Зависит от Т2.3.
- [ ] **Т2.9 Квитанции идемпотентности (§4.4, §18 Фаза 1 п.4).** Сейчас
  сервер считает свой `dedupe_key` сам, а клиентского `idempotency_key` и
  сохранённой квитанции нет вовсе. Требуется: приём принимает
  `idempotency_key` от клиента, результат операции сохраняется, и повтор с
  тем же ключом возвращает **ту же** квитанцию, а не новый расчёт.
  Регрессия Т2.7 про повтор после потерянного ответа этого не заменяет: она
  проверяет, что дубля нет, а не что ответ стабилен. Размер M. Зависит от
  Т2.1. Затрагивает `scripts/mara_ingest.py`, `scripts/contextd.py` и
  `android/` (клиент должен ключ генерировать и переиспользовать при
  повторе) — значит и гейт Г2.
- [ ] **Т2.8 Миграция на doctor.** Гейт Г4. Зависит от Т2.0: смена
  авторитета на базу без перенесённого состояния — потеря правок
  владельца. Кто: агент+гейт.

### Волна 3 — приём без тихих потерь (агент, M)

Идёт параллельно волне 2 там, где не пересекается по файлам.

- [ ] **Т3.1 Хвост issue #39, подтверждённая часть.** Семь пунктов:
  мохнатая кириллица в `log_line` (CPython декодирует строку запроса как
  iso-8859-1 — чинить в одном месте, `log_line`, а не у трёх зовущих);
  счётчики отказов и 413 не видны в сверке; проверка «волт пропал»;
  заставы на все 13 проверок сверки; ограничение частоты коррекций;
  `ПРОБА=3` — полнота зеркала; семантика `mara_mobile_pending_uploads`.
  Размер M. Каждый пункт — отдельный тест.
- [ ] **Т3.2 Список разрешённых MIME + нюх содержимого.** §6.2. Сейчас
  расширение из тела запроса попадает в путь после чистки, но тип
  содержимого не проверяется. Размер S.
- [ ] **Т3.3 Хвост #73.** Трата трафика на 503, потолок потоков на
  устройство, тело > 8 МиБ, `setBackoffCriteria` в `SyncWorker` (Kotlin —
  гейт Г2). Размер S.
- [ ] **Т3.4 Различитель дедупа WhatsApp.** P0-7, §17.1 последний пункт:
  два подряд одинаковых сообщения — два события, а не одно. Kotlin +
  сервер. Размер M. Гейт Г2.

### Волна 3б — операционная надёжность (§17.3, Фаза 2) — агент, M

Большая часть закрыта (P0-5, P0-6, потоковая загрузка, `core-backup.py`),
но чекбоксы §17.3 держатся на доказательствах, которых нет в документах.

- [ ] **Т3б.1 Учение восстановления в изоляции.** Один прогон
  восстановления **полного** состава по §5.3 на чистом каталоге, с записью
  в `docs/RUNBOOK-restore.md`: что делали, сколько заняло, что проверили.
  §17.3 п.5. Размер S — если пройдёт с первого раза.
  **Идёт последней в волне: зависит от Т3б.5 и Т3б.6.** Учение по
  неполному составу проверяет неполный состав — и закрывает чекбокс §17.3
  п.5 ложно. Порядок внутри волны: Т3б.3 → Т3б.5 → Т3б.6 → Т3б.1 → Т3б.2,
  Т3б.4 независима.
- [ ] **Т3б.2 После восстановления: пересборка проекций, сверка блобов,
  открытие образца evidence.** §17.3 п.6. Зависит от Т2.6 и Т2.4.
  Размер S.
- [ ] **Т3б.3 Проверка манифеста резервной копии.** §17.3 п.4. Размер S.
- [ ] **Т3б.4 Доказательство ограниченной памяти при загрузке.** §17.3
  п.1 требует не «потоково по коду», а измеренный профиль: залить файл
  на сотни мегабайт и показать, что RSS не растёт линейно. Размер S.
- [ ] **Т3б.5 Дыры в составе бэкапа по §5.3.** ADR-0005 сам перечисляет,
  чего в `core-backup.py` нет, и учение восстановления этого не заменяет:
  копия за пределами doctor (§5.3 п.8), отдельный зашифрованный бэкап
  secrets/config с описанным восстановлением (п.5), `host` и класс
  хранения в манифесте, ротация поколениями вместо `keep-N`, частый
  локальный снимок реестра между суточными прогонами. Размер M. Трогает
  `scripts/core-backup.py`, `docs/backup-core.md`,
  `docs/adr/0005-sqlite-durability-backup.md`.
- [ ] **Т3б.6 Ранбук восстановления до девяти шагов.** В
  `docs/backup-core.md` шесть шагов из девяти, которых требует §5.3: нет
  остановки писателей, пересборки проекций и выборочной проверки stable id
  и evidence. Дописать и проверить их в учении Т3б.1, а не после.
  Размер S. Зависит от Т2.2 и Т2.6 (проверять нечего, пока нет стабильных
  ID и пересборки).

### Волна 4 — рекордер и здоровье (§17.2, Фаза 3)

- [ ] **Т4.1 `RecorderProvider`.** Абстракция + адаптер ACR Phone/APH +
  манифест конфигурации. §7. Kotlin. Размер M. Гейт Г2.
- [ ] **Т4.2 Health workflow: после `BOOT_COMPLETED` и периодически.**
  §18 Фаза 3 п.3 требует и того, и другого; §7 требует штатного
  планировщика Android (`WorkManager`), а не собственного таймера.
  Проверяет доступ к папке, разрешения, сигналы провайдера. Периодическая
  проверка нужна затем, что разрешение или доступ к папке отбирают и без
  перезагрузки — тогда единственная проверка на загрузке уже соврала.
  **Запрет ложного зелёного:** до успешного контрольного звонка статус не
  `healthy`. Размер M. Гейт Г2.
- [ ] **Т4.3 Сопоставление «последний звонок ↔ последняя запись».**
  `CallLogMatcher` уже есть — довести до предупреждения в измеренный срок,
  без дублей. Размер M.
- [ ] **Т4.4 Инструкция восстановления** + отдельный исследовательский
  вердикт `RES-REC-001` (`Server + Secret` — не называть webhook без
  доказательства, §22.10). Размер S. Документ.
- [ ] **Т4.6 `RES-REC-002` — способы передачи ACR.** SFTP/WebDAV/FTPS/
  устройство по отдельности; обычный FTP — только для понимания, не в
  production (§7.3). Проверить автозапуск, стабильность имён, дозагрузку,
  потерю сети, поведение после перезагрузки. Размер S. Документ.
- [ ] **Т4.7 `RES-REC-003` — надёжный захват после перезагрузки.**
  Критерий — повторяемая серия «перезагрузка → звонок», а не «однажды
  сработало». Опирается на мешок М4. Размер S. Документ.
- [ ] **Т4.5 Полевая матрица R01–R14.** Кто: **владелец** (мешок М4).
  Именно она решает, autonomous или degraded (§23).

### Волна 5 — обработка (§17.4, Фаза 4) — агент, L

- [ ] **Т5.1 Сегменты ASR: временные метки и стабильные ID.** Опора для
  evidence из Т2.4. Размер M.
- [ ] **Т5.2 Диаризация: замер `RES-ASR-001`.** Сначала измерить, потом
  выбирать. Локально, без облака (§7.3). Размер M.
- [ ] **Т5.3 Атрибуция говорящего + коррекция.** Не выдумывать личность;
  «неизвестный» — законный ответ. Размер M.
- [ ] **Т5.4 Извлечение по схеме.** Просьба ≠ обещание; owner ≠ requester.
  Размер M.
- [ ] **Т5.5 Регрессионный корпус.** Согласованные или синтетические
  записи; в Git аудио не кладём (§7.3, §18). Размер M.
- [ ] **Т5.0 Происхождение каждого производного результата (§9).** Каждый
  результат хранит имя и версию модели, конфигурацию, хеш входа и версию
  конвейера; переобработка создаёт **новую** производную ревизию, исходный
  блоб не меняется; промпты и правила версионируются. Без этого «почему в
  карточке это» неотвечаемо, а переобработка неотличима от первой
  обработки. Размер M. Делается **до** Т5.1: сегменты без происхождения
  придётся переделывать.
- [ ] **Т5.6 Проверка evidence на живых данных.** Ссылка открывает нужный
  фрагмент. Размер S. Зависит от Т2.4 и Т5.1.

### Волна 6 — контекст и MCP (§17.4, Фаза 5) — агент, M

- [ ] **Т6.1 Версионирование паков + `supersedes` + верхняя отметка.**
  Белый список полей пака (`ПОЛЯ`) придётся расширить — это решение
  владельца (М5). Размер M.
- [ ] **Т6.2 Паки `now`, person, project, query.** Размер M.
- [ ] **Т6.3 Контракты MCP: read/query/correction/доменные команды.**
  Размер M.
- [ ] **Т6.4 Области доступа, подтверждения, аудит.** §11, ADR-0009 есть.
  Размер M.
- [ ] **Т6.5 Долгая сессия Hermes.** Проверка, что закрытие обязательства
  немедленно видно в новом `now`-паке. Размер S.

### Волна 7 — вычисления (§17.5, Фаза 6) — агент, M

- [ ] **Т7.1 Схема задач: способности и ресурсы.** Размер S.
- [ ] **Т7.2 Протокол воркера и сердцебиения.** Размер M.
- [ ] **Т7.3 Маршрутизация по умолчанию на bigpc.** Размер S.
- [ ] **Т7.4 Застава GTR по Minecraft.** У GTR нет GPU и на нём крутится
  Minecraft — тяжёлая задача туда не уходит при активной игре. Размер S.
- [ ] **Т7.5 Doctor работает при выключенных bigpc и GTR.** Первый пункт
  §17.5, проверяется отключением. Размер S. Проверка в мешке М4 (P09–P11).
- [ ] **Т7.6 Наблюдаемость расписания, отмена и повтор.** §18 Фаза 6 п.5:
  видно, где стоит задача и почему; отмена доходит до воркера, а не только
  до очереди; повтор после падения воркера не теряет и не удваивает работу.
  Без этого зависшая задача на выключенном bigpc не отличается от идущей.
  Размер M. Зависит от Т7.2.

### Волна 8 — Control Plane и Agent Observatory (§17.5, Фаза 7) — агент, L

**Не начинать раньше закрытия волны 2** (§22.7). Сейчас в репозитории нет
ни `web/`, ни `package.json` — волна строится с нуля.

- [ ] **Т8.0 `RES-OSS-001` — журнал решений о переиспользовании.**
  React/TanStack/OTel/MCP SDK: лицензия, поддержка, безопасность,
  архитектурная пригодность. Не копировать код только за похожий вид.
  Graphiti/Meilisearch/Langfuse и векторные/графовые базы — отдельный ADR
  с замером. §20, §18 Фаза 8. Размер S. Делается **до** Т8.1.
- [ ] **Т8.1 Только чтение: дашборд и здоровье.** React + Vite, §12.3.
  Размер M. Зависит от Т8.0.
- [ ] **Т8.2 Память, evidence, история.** Размер M.
- [ ] **Т8.3 Правки с проверкой версии и UX конфликта.** Опирается на
  Т2.3. Размер M.
- [ ] **Т8.4 Экран восстановления рекордера.** Размер S.
- [ ] **Т8.5 Резервные копии и доказательство восстановления.** Размер S.
- [ ] **Т8.6 Agent Observatory.** Прогоны, бюджеты, изменения, PR — с
  затиранием чувствительного. §14. Размер M.
- [ ] **Т8.7 Реестр виджетов и отчётов.** Версионированный контракт,
  произвольный SQL запрещён. §12.2. Размер M.

### Волна 9 — наблюдаемость, управление, приёмка

- [ ] **Т9.1 §15.1: метрики, санитизированные логи, тревоги.** Размер M.
- [ ] **Т9.2 Три недостающих ADR: 0004, 0010, 0011.** §21 требует 12,
  есть 9. Размер M.
- [ ] **Т9.3 Перевести ADR из «предложен» в «принят».** Все девять сейчас
  «предложен» — то есть формально ни одно решение не принято. Размер S.
- [ ] **Т9.4 Закрыть чекбоксы §17.1–§17.6** — каждый либо галочкой с
  доказательством, либо явной строкой «не закрыт, потому что…». Ни один
  пункт, требующий полевого E2E, не закрывается ссылкой на юнит-тест
  (§17.6, последний пункт). Размер M.
- [ ] **Т9.0 Повторный выкат перед приёмкой.** Кто: агент (полномочия
  переданы 2026-09-11). Волна 1 выкатывает `main`
  и ставит APK **до** того, как волны 2–8 перепишут `contextd`, схему
  реестра, приложение, рекордер и Control Plane. Прогонять §23 на том
  выкате — проверять прошлую версию. Перед Т9.4 и Т9.5: свежий выкат по
  `docs/RUNBOOK-deploy.md`, свежий APK на телефон, миграция Т2.8
  выполнена, `install-cron.sh --apply`, рестарт `contextd`, проверка что
  источники не молчат. Гейт Г5 действует и здесь. Размер S.
- [ ] **Т9.5 Прогнать цикл §23 целиком** и записать вердикт: autonomous
  или degraded. Требует мешка М4 и Т9.0. Размер M.
- [ ] **Т9.6 Обновить `docs/current-state-audit.md`** под новую
  реальность и закрыть issue #1 и #39. Размер S.

---

## 7. Мешки владельца

Собраны так, чтобы каждый мешок делался за один присест и в одном месте.
Порядок важен: М1 и М2 разблокируют почти всё остальное.

### Мешок М1 — браузер, ~15 минут (делать первым)

1. ~~Защита `main`~~ — **сделано мной 2026-09-11**, владельцу делать нечего.
2. **Telegram**: my.telegram.org → `api_id` и `api_hash`. Прислать мне
   способом, который не оставляет их в переписке (положить в
   `/etc/mara/tdlib.env` самому — лучший вариант).
3. **Google Cloud**: OAuth client типа Desktop + **Publish app**.
   Без публикации refresh-токен умирает через 7 дней.
4. **Ротация ключей OpenAI и Anthropic**, если они ранее светились в
   истории проекта. Ключ OpenRouter владелец уже менял — его не трогаем.
5. **Remotely Save**: исключить `*/data.json` из синхронизации.
6. **Copilot на маке**: «Index vault» — **никогда**.

### Мешок М2 — терминал doctor (большая часть снята 2026-09-11)

Выкат, крон и рестарт делаю я сам — полномочия переданы. Владельцу остаются
только шаги, требующие его браузера и его аккаунтов: разовый `--login` у
TDLib и Gmail после того, как я положу на место `api_id`/`api_hash` и
OAuth-клиент из мешка М1.

Этот мешок повторяется дважды: один раз сейчас (волна 1), второй — перед
приёмкой (Т9.0), когда волны 2–8 перепишут код. Второй раз добавится
миграция Т2.8, и только с разрешением из М5 п.15.

1. Выкат `main` по `docs/RUNBOOK-deploy.md` (или разрешить мне делать это
   самому — см. М5 п.1).
2. `systemctl restart contextd` — снимает долг рестарта по #47, #67, #71,
   #73, #82.
3. TDLib: положить `api_id`/`api_hash`, разовый `--login`,
   `systemctl enable --now tdlib-ingest`.
4. Gmail: `ssh -L 8765:127.0.0.1:8765 doctor`, `gmail_ingest.py --login`,
   затем `--backfill --days 30`.

### Мешок М3 — телефон, ~40 минут

1. Поставить свежий APK (`doctor:~/dist/mara-capture-debug.apk`).
2. Разрешения: доступ к уведомлениям, снятие оптимизации батареи, доступ
   к папке записей.
3. Настроить ACR по `USER-MANUAL-STEPS.md` §2 и ТЗ §7.2.
4. Спарить (`contextd.py --pair`), нажать «Проверка связи».
5. Отчёт мастера — прислать **после затирания номера телефона и имени
   контакта** (или прислать мне на doctor, а не в переписку).
6. Контрольный звонок + проверка WhatsApp/SMS.

### Мешок М4 — полевая матрица, 2–3 присеста

Это единственное, что нельзя подделать кодом, и именно оно определяет
вердикт §23.

1. R01–R14 из §16 — по одному пункту, с отметкой времени.
2. **R05/R06 — перезагрузка телефона**: стартует ли запись сама.
3. §16.4 — оценка качества записи обеих сторон.
4. P09/P10/P11 — bigpc выключен / включён / Minecraft активен на GTR.

### Мешок М5 — решения текстом (можно ответить одним сообщением)

1. ~~Передать ли мне выкат на doctor?~~ — **да, 2026-09-11**. Гейт Г5 снят,
   мешок М2 сократился до пунктов, где нужен браузер владельца.
2. Расширение белого списка полей пака `ПОЛЯ` под `supersedes`.
3. Политика ограничения частоты коррекций.
4. Issue #59: сузить §8.3 или реализовать presidio-anonymizer целиком.
5. Issue #12 (STT: «Мара» → «Марина») — чинить или жить с этим.
6. Issue #28 — какой вариант атрибуции события с блобом.
7. Затирать ли номер телефона в отчёте мастера автоматически.
8. Перегонять ли разговоры с Марой в память.
9. Статусы ADR: перевести девять «предложен» в «принят» — какие именно.
10. Судьба `/tmp/edge`.
11. Имя рабочего репозитория в `config/git-repos.txt`.
12. Включать ли AI-воркера на GTR.
13. Приоритет Control Plane: сейчас или после волны 5.
14. ~~PR #85 и два рабочих дерева рядом с ним~~ — **не трогать**, решение
    владельца 2026-09-11. Чужая работа в процессе.
15. **Разрешение на миграцию схемы реестра на doctor** (Т2.8, гейт Г4) —
    даётся после свежей резервной копии и пройденного учения
    восстановления. Без него Т2.8 стоит.

### Мешок М6 — низкий приоритет, когда будет настроение

1. Остатки Tailscale.
2. Obsidian на маке и Windows.
3. Чистка `/tmp` на beta-pi (20 191 каталог `tmp*`).

---

## 8. Проверка целиком (как понять, что готово)

**Уровень 1 — гейт.** Г1 зелёный, CI зелёный, счёт тестов растёт и назван.

**Уровень 2 — приёмка §17.** Все шесть подразделов, каждый пункт с
доказательством. Пункты, требующие поля, закрываются полем.

**Уровень 3 — цикл §23.** Один непрерывный прогон:

```
перезагрузка телефона → здоровье не врёт → обычный звонок → обе стороны
разборчиво → файл однозначно связан со звонком → сеть падает и
возвращается → загрузка завершается ровно один раз → ASR и говорящие дают
проверяемые сегменты → обязательство извлечено с правильными
owner/requester/срок → evidence открывает нужный фрагмент → владелец
правит и закрывает → ревизия и аудит без потерянного обновления → новый
пак немедленно вытесняет старый → Claude, Codex, Hermes и PWA видят одно
состояние → бэкап восстанавливает реестр и блобы, Markdown пересобирается
```

Вердикт записывается в `docs/current-state-audit.md` одним из двух слов:
**autonomous reliable memory capture** или **degraded prototype with
monitored recovery**. Второе — не провал, но и не маскируется под первое.

---

## 9. Самопроверка плана (сделана 2026-09-11)

* **Покрытие ТЗ.** §17.1 → волна 2 + Т3.4. §17.2 → волна 4 + М4.
  §17.3 → волна 3б (включая Т3б.5 и Т3б.6 — дыры §5.3, которые ADR-0005
  перечисляет сам; учение Т3б.1 идёт последним, иначе проверяет неполный
  состав). §5.1 → Т2.1б, §5.2 → Т2.1в, §9 происхождение → Т5.0. §17.4 → волны 5 и 6. §17.5 → волны 7 и 8.
  §17.6 → Т0.5, Т0.6, Г3. §4.2 набор сущностей → критерий Т2.1;
  §4.8 восстановимость волта из реестра → Т2.0 (без неё пересборка стирает
  правки владельца, а не восстанавливает их). §18 Фаза 0 → Т0.5, Т0.6, Т0.8 и уже сделанный аудит;
  Фазы 1–7 → волны 2–8; Фаза 8 → Т8.0. Фаза 1 п.4 → Т2.9 (квитанции
  идемпотентности — не то же, что дедупликация); Фаза 3 п.3 → Т4.2 (обе
  проверки, и по загрузке, и периодическая); Фаза 6 п.5 → Т7.6.
  §20: RES-REC-001 → Т4.4, RES-REC-002 → Т4.6, RES-REC-003 → Т4.7,
  RES-ASR-001 → Т5.2, RES-OSS-001 → Т8.0. §21 → Т9.2, Т9.3.
  §23 → Т9.5. Непокрытых пунктов не осталось.
* **Заглушек нет.** Каждая карточка называет файл или подсистему и
  критерий, по которому её закроют.
* **Выкат не отстаёт от кода.** Т9.0 переносит выкат и APK на момент
  перед приёмкой: волна 1 включает поток данных, но §23 прогоняется на
  свежем дереве, а не на том, что выкатили до волны 2.
* **Порядок непротиворечив.** Т2.3 раньше Т8.3 и Т2.7; Т2.4 раньше Т5.6;
  Т5.1 раньше Т5.6; Т8.0 раньше Т8.1; Т2.6 раньше Т3б.2. Волна 8 стоит
  после волны 2 по §22.7, волна 1 (коннекторы) — единственное исключение,
  и оно законно: TDLib и Gmail уже написаны, их не «добавляют», их
  включают.
* **Публичность.** В документе нет домашних адресов, системных имён
  пользователей, имени владельца, номеров телефонов и имён личных папок.
  Путь в гейте Г1 записан как `~/.mara-tmp`: то, что имя пользователя уже
  встречается в четырёх скриптах репозитория, не делает новое вхождение
  допустимым (замечание Codex, круг 1).

---

## 10. Что делаю прямо сейчас, если никто не отвечает

~~Т0.1~~ → ~~Т0.2~~ → ~~Т0.3~~ → ~~Т0.5~~ → ~~Т0.6~~ → Т3.1 → Т3.2 → Т0.8 →
Т2.1 → Т2.2 → Т2.3 → …

То есть: разгружаю очередь, фиксирую план, чиню приём, строю авторитет
реестра. Мешки М1–М4 ждут владельца и не блокируют ни одну из этих задач —
блокируют они только волны 1, 4 (пункт Т4.5) и финальный вердикт §23.
