# Решения по открытым вопросам ТЗ

Ответы Сергея на §15 и на то, что вылезло по ходу. Дата — 2026-08-31/09-01.

## §15.1 — репозитории для git-ingest

**Все, включая форки.** Причина: в форках есть его собственные правки, терять их
не хочется. Следствие для реализации: по форкам берём только коммиты его
авторства, апстримные мерджи в заметки не тащим.

## §15.2 — почта

Личный ящик не слушаем: там почти только спам, токены на него тратить незачем.
Интерес — к **рабочему** `i.petrov@example-corp.ru` (корпоративный Google
Workspace).

**Не сделано, ждёт его действий.** Обычный пароль Google к IMAP не пустит,
нужен app-password при включённой 2FA, и его может запретить админ домена.
Пароль в переписку не берём: он корпоративный, даёт доступ не только к почте,
и содержимое ящика принадлежит не только ему.

Глубина: **только заголовки** (от кого, тема, дата). Тела — никогда без
отдельной команды.

## §15.3 — геолокация

**Не собираем.** Home Assistant на doctor есть (`iot-homeassistant`, `:8123`),
гео из него взять можно в любой момент, но смысла в хранении Сергей не видит.
OwnTracks не ставим. Пункт из §6.6 вычёркивается.

## §15.4 — Ваня

**Доступа к волту нет вообще** — ни своего волта, ни read-only подмножества.
Мара рассказывает данные из волта **только Сергею**.

Следствие для этапа 3: Мара обязана различать собеседника. Инструменты чтения
волта включаются по личности говорящего, а не по профилю сессии.

Ваня — из §11 самого ТЗ, не выдумка исполнителя.

## §15.5 — порог создания сущности

**При первом упоминании.** Согласуется с правилом §7.1 «сомневаешься —
фиксируй». Двести карточек людей — это двести мелких markdown-файлов, нагрузки
никакой.

## Эмбеддинги (не из §15, вылезло по ходу)

**bge-m3 через Ollama**, локально на doctor.

Замер на самом doctor (i5-7200U, 4 потока, без GPU), кусок 3000 символов:

| модель | заметка 3000 симв | запрос | dim |
|---|---|---|---|
| bge-m3 | 11.3 с | 0.28 с | 1024 |
| qwen3-embedding:0.6b | 25.6 с | 0.42 с | 1024 |

На лидерборде MTEB multilingual выигрывает qwen3 (~64.3 против 63.2), на этом
железе — bge-m3, вдвое с лишним. Разница в качестве меньше балла, разница в
скорости — в 2.3 раза. Обе модели названы в §8.2, так что это выбор внутри ТЗ,
а не отступление.

## Индекс Copilot в R2

`.obsidian/copilot-index-*.json`, 85 МБ, 76% объёма бакета. Векторный индекс
плагина Obsidian Copilot в формате Orama, эмбеддинги от
`openai/text-embedding-3-small` через OpenRouter, датирован 3 июля.

Переиспользовать нельзя: другое векторное пространство, локальной bge-m3 по
нему не поискать. Сносить бессмысленно: у плагина `enableIndexSync = true`,
Obsidian зальёт обратно. **Оставили как есть**, синк его игнорирует.

⚠️ К этапу 3: у Copilot `embeddingModelKey = openai/text-embedding-3-small`
через OpenRouter. Когда Мара начнёт писать в `daily/`, Copilot проиндексирует
и дневник, включая `sensitive: true`, и §13.7 сломается настройкой плагина.
Либо переводим Copilot на локальную модель, либо исключаем `daily/` из его
индексации.

## Структура волта

Вариант **Б**: навести порядок в существующем волте — лишнее удалить, нужное
разложить по §3 и дистиллировать. План разбора — в [`vault-cleanup.md`](vault-cleanup.md).

## 2026-09-01. Двенадцать залипших объектов в R2

`GET` и `LIST` по ним работают, `PutObject` и `DeleteObject` отдают
`HTTP 500 InternalError` — стабильно, не по разу. На ключе, созданном
секундой раньше в том же бакете, delete проходит: сломан не бакет, а эти
конкретные объекты. Все двенадцать — остатки реструктуризации §3, их
содержимое лежит в волте под новым путём (совпадает и по размеру) либо
осталось в истории git, так что ничего не потеряно.

Обошли исключением в `config/r2-filters.txt`: bisync их не видит и не тянет
обратно в волт. `scripts/r2-retry-orphans.sh` пробует удалить их снова —
когда Cloudflare отпустит ключи, строки из фильтров можно убрать (это правка
фильтров, потребуется новый `--resync`).

Тот же `--filter-from` добавлен в проверку лишнего перед resync в
`vault-r2-sync.sh`: без него guard спотыкался о собственные исключения.

## 2026-09-01. Восстановление реальных дат заметок

`occurred` требует даты события, а не импорта. Настоящие mtime заметок были
затёрты: Basic Memory переписал все 125 файлов 31 августа, git волта начат
тогда же, в R2 лежит время загрузки. Единственный уцелевший источник —
индекс плагина Copilot (снимок от 3 июля, 88 МБ), где у каждой заметки
сохранены `ctime` и `mtime` с машины пользователя. Достали
`scripts/copilot-dates.py`, старые пути сопоставили с новыми по переименованиям
из коммита реструктуризации.

Покрытие: 56 заметок из 101. Остальным `occurred` **не проставлен**. Забить
всем датой импорта было бы проще, но это навсегда сломало бы вьюху «что я
делал за неделю» — 101 заметка одним днём. Нет даты — нет поля.

Заодно подтвердилось, что Copilot гонял содержимое заметок в
`openai/text-embedding-3-small` (поле `embeddingModel` в каждой записи
индекса) — то самое, о чём предупреждение по §13.7.

## 2026-09-01. Вьюхи Bases живут в `views/`

В §3 такой папки нет. `_system/` — служебное состояние, а вьюхи это
пользовательский слой, им там не место. Четыре `.base` из §9 лежат в
`views/` в корне волта.

## 2026-09-01. Диагноз по R2 был шире правды

Сначала я записал поломку как «бакет не принимает перезапись существующих
ключей». Это оказалось неверно. Перезапись давно лежавшего
`.obsidian/plugins/copilot/data.json` (от 2026-08-20) прошла с первого раза, и
последующее удаление тоже. Контрольный опыт на свежем ключе — запись,
перезапись, удаление — три раза `rc=0`.

Но и вторая формулировка — «испорчены ровно четырнадцать объектов» — не
выдержала. `--resync` тут же нашёл ещё девять, и `kb/howto/atman/ENDPOINTS.md`
падает с 500 при одиночной перезаписи, а не только в пакете.

Что установлено твёрдо: здоровый ключ переживает запись, перезапись через 200
секунд и удаление; испорченный не переживает ничего, сколько ни повторяй.
Отличить их заранее по метаданным не выходит. Сколько их всего — меряется
принудительной перезаписью всего волта, а не оценивается на глаз.

Финальный замер: принудительная перезапись всех 184 файлов волта, дважды
подряд — оба раза падают одни и те же 14, расхождений ноль. Свежий бакет из 40
объектов пережил перезапись через 5 минут и удаление без единой ошибки.

Итог: порча привязана к объектам, детерминирована, локальна для бакета.

Урок: я дважды объявил диагноз по одному удачному наблюдению — сперва завысил
(«весь бакет не принимает перезапись»), потом занизил («ровно 14 старых
объектов, бакет здоров»). Оба раза хватило бы одного прогона тем же объёмом,
каким пользуешься. Он занял три минуты и дал однозначный ответ.

## 2026-09-01. Живые API-ключи лежали в волте и уехали в R2

Настройки плагина держали ключи внешних сервисов,
Anthropic и OpenRouter. Файл трекается в git волта и синкался в R2 — то есть
три рабочих ключа лежали в облачном бакете.

Что сделано: `- /.obsidian/plugins/*/data.json` в `config/r2-filters.txt`,
копия из R2 удалена. Настройки плагинов теперь не покидают устройство —
невелика потеря, они и так у каждой машины свои.

Что осталось владельцу: **ключи скомпрометированы и подлежат отзыву**. Удаление
из бакета не отменяет того, что они там лежали.

Оттуда же — доказательство по §13.7: `embeddingModelKey` у Copilot стоит
`openai/text-embedding-3-small|openrouterai`, а `qaExclusions` исключает одну
папку `copilot`. Значит весь остальной волт, включая `daily/`, индексируется
через OpenRouter в облако.

## 2026-09-01. Переезд на бакет `mara-vault`

Владелец решил переезжать. Сделано: новый бакет, `REMOTE` в
`scripts/vault-r2-sync.sh` перецелен, состояние bisync снесено, `--resync`
залил 184 файла. Приёмка: принудительная перезапись всех 184 — ноль ошибок,
включая все 14, что в старом бакете были заперты навсегда. Обычный прогон
bisync — 4,5 секунды против 29 на больном бакете.

Из фильтров убраны 28 строк исключений (14 испорченных файлов волта + 14 сирот,
живших только в старом бакете). Сироты в новый бакет не поехали — их нет в
волте, а волт тут источник (§1).

Старый бакет не удалён. Во-первых, в нём заперты объекты, которые физически не
удаляются. Во-вторых, Remotely Save на телефоне, маке и винде всё ещё пишет
туда, и до перецеливания устройств `scripts/r2-legacy-pull.sh` односторонним
`copy --update` забирает оттуда новые заметки. Без этого заметка, созданная на
телефоне ночью, просто пропала бы: боевой синк уже смотрит в другой бакет.

Забор односторонний намеренно. Двусторонний bisync к двум бакетам разом — это
два процесса, наперегонки правящих один волт (§13.8), плюс шанс, что старый
бакет навяжет устаревшую копию. `--update` копирует только то, что в бакете
новее, а испорченные 14 объектов там заморожены и всегда старее.

## Этап 2: карточка сессии собирается без LLM (1 сентября)

ТЗ §6.1 говорит «hook на SessionEnd → дистиллятор → `kb/sessions/<id>.md`».
Дистиллятора в хуке нет, и это осознанно.

На doctor нет генеративной модели — только `bge-m3` и `qwen3-embedding`, четыре
ядра и 15 ГБ без GPU. А приёмка этапа 2 прямо требует, чтобы при отключённой
сети сырьё писалось и задача уходила в очередь. Хук, который ходит в облако,
этой проверки не проходит по определению.

Поэтому разделено на два такта:

1. **Хук, механически.** Заголовок (Claude Code сам кладёт `ai-title` в
   транскрипт — готовый, бесплатный), дата события, каталог, ветка, счётчики,
   ссылка на сырьё. `distilled: false` + задача в `_system/queue/`.
2. **Крон-обработчик очереди.** Ходит в OpenRouter, переписывает тело той же
   карточки, ставит `distilled: true`.

Карточка полезна уже после первого такта: сессия находится поиском и попадает
в `daily/` по `occurred`. Это ровно слой 2 из §8.4, а не заглушка.

## Спул вместо прямой записи в волт (1 сентября)

Хук крутится там, где Claude Code (beta-pi, мак), а волт на doctor. Первый
вариант был «нет сети → пишем задачу в очередь». Он неправильный: очередь тоже
на doctor, недоступен doctor — недоступна и очередь.

Клиент пишет в `~/.local/state/mara/spool/` — дерево той же формы, что волт, —
и оттуда `rsync -a --remove-source-files`. Удаляется только то, что доехало,
значит спул сам себе очередь ретраев, отдельного механизма не нужно. Проверено:
доставка на несуществующий хост оставила три файла в спуле, следующий запуск
хука донёс все три.

`rsync` пишет во временный файл и переименовывает, поэтому автокоммит (`*/15`)
и bisync (`*/5`) не могут поймать полузаписанную карточку.

## Порог Presidio 0.4, а не 0.5 (1 сентября)

Самопроверка редакции ловила почту и ключи, но пропускала `+7 916 123-45-67`.
Причина не в регэкспах: `PhoneRecognizer` из коробки работает с регионом US,
российский номер он всё-таки находит, но со счётом 0.4 — ровно под порогом.

Исправлено двумя правками: распознаватель пересоздан с `supported_regions=("RU","US")`,
порог опущен до 0.4. Ложное срабатывание стоит одного затёртого числа, пропуск
стоит номера в облаке.

Отдельно: `registry.remove_recognizer("PhoneRecognizer")` снимает все языки
сразу, поэтому вызывать его нужно один раз до цикла. В цикле вторая итерация
снимала то, что добавила первая, и для русского языка распознавателя не
оставалось вовсе.

## MCP через ssh-туннель, а не наружу (1 сентября)

§11: MCP только на loopback, никогда `0.0.0.0`. Basic Memory слушает
`127.0.0.1:8787` на doctor, а Claude Code и Codex живут на других машинах.
Открыть порт наружу нельзя, поэтому systemd-юнит `mara-mcp-tunnel.service`
держит `ssh -N -L 127.0.0.1:8787:127.0.0.1:8787 doctor`.

Рассматривался вариант проще — MCP через stdio: `ssh doctor basic-memory mcp`,
без туннеля и без юнита. Отклонён: каждая сессия поднимала бы свой процесс
Basic Memory со своим файловым вотчером, и несколько вотчеров писали бы в один
волт. Это ровно то, что запрещает §13.8. Туннель переиспользует один уже
работающий сервер.

## Имена людей в облако уходят (1 сентября)

§8.3 требует PII-редакции перед любым облачным вызовом. Буквальное чтение —
резать и `PERSON`. Но §5 и §7 требуют обратного: дистиллятор обязан доставать
людей из сессии и заводить карточки в `entities/people/`, а этап 4 строит по
ним граф. Вырезав имена, мы получаем базу знаний без людей.

Решение: режем то, что опасно — ключи, токены, приватные ключи, почту,
телефоны, карты, IBAN, IP, крипто-адреса. Имена, организации и места остаются.
Если решение поменяется, правится `PII_ENTITIES` в `scripts/redact.py`.

Неполная редакция при этом **не повод отправить как есть**: если Presidio не
импортируется, worker пишет в лог и держит очередь.

## Маска на mac-mini: что там появилось от соседа (1 сентября, ночь)

Пишет второй агент, который занимается железом маски. Всё ниже — на твоей
территории, поэтому в известность ставлю, а не молча.

**Секрет лежит вне репозитория.** Пароль от Wi-Fi для платы — в
`~/mask/wifi.json` на маке, `chmod 600`, в гит не попадает и не должен. Класть
его в волт или в конфиг не надо: ты недавно вычищал секреты историей, второй раз
на те же грабли не идём.

**На маке слушается порт 9999.** `~/mask/listen.py` принимает звук с платы
(она в сети под 192.168.1.149, ESP32 только 2,4 ГГц, сеть `Serenity_slow`) и
режет речь на куски в `~/mask/heard/*.wav`. Запускается руками, никакого
launchd я не заводил. Увидишь питон на 9999 или незнакомую железку в сети —
это она, не вторжение.

**Hermes: правил SOUL.md.** Добавил три пункта про то, что голосом Мара
отвечает только по просьбе, до появления маски. Бэкап рядом:
`~/.hermes/SOUL.md.bak.20260901_032041`. Хозяин разрешил править явно, но файл
твой, поэтому говорю. `LORE.md` и `USER.md` не трогал. Гейтвей перезапущен
через `launchctl`, поллинг телеграма живой.

**Громкость мака трогал.** На время проверки микрофона опускал с 55 до 20 и
вернул обратно. Если увидишь 20 — значит проверка оборвалась на середине,
верни на 55.

**Конфиг Гермеса: одна ручка.** `display.tool_progress` был `all`, стало `"off"` —
это лента вызовов инструментов, которая сыпалась в телегу («Scheduling create
×12»). Бэкап `~/.hermes/config.yaml.bak.20260901_122831`, разница в одну строку,
гейтвей перезапущен, поллинг подтверждён в 12:28:55. Кавычки обязательны: голое
`off` YAML читает как булев `false`, а код ждёт строку.

**Чего ещё нет.** Распознавания речи на большом компьютере не существует: на
порту 8770 только синтез (`/say`, `/warm`, `/stop`, `/lex`), ручки
`/transcribe` там нет. Нарезанные куски пока никуда не уезжают и лежат на диске.

## Маска заговорила: распознавание и мостик в Гермеса (1 сентября, день)

Продолжение ночной записи выше. Тот абзац «чего ещё нет» устарел: распознавание
на большом компьютере теперь есть, и услышанное доезжает до Мары.

**`/transcribe` на 8770.** Отдельной службы не заводил, слух подсажен к той же,
что говорит голосом Мары: `POST /transcribe`, тело — сам wav, ответ `{"text"}`.
Модель `openai/whisper-large-v3-turbo`, 1,5 ГиБ, поверх уже стоявших torch и
transformers — `faster-whisper` не брал намеренно, он тянет свой рантайм
CTranslate2, а на свежей Blackwell-карте у него может не оказаться ядер.
Замок и жнец общие с голосом, таймеры простоя раздельные: карта одна и она же
рабочая станция хозяина. Веса легли в `/root/.cache/huggingface` — туда, куда
служба смотрит без `HF_HOME`; вторую копию в `/root/tts/hf` не плодил.
Замеры на живых записях: холодный запрос 13,7 с, тёплый 0,07..0,21 с.

**Мостик — плагин, не патч.** `~/.hermes/plugins/mask-ear/` (исходник в моём
репозитории, `mask/plugin/`) следит за `~/mask/heard` и на новый `.txt` зовёт
штатную `PluginContext.inject_message`. Кода Гермеса не трогал.

**Что я поменял у тебя в конфиге.** Бэкап `~/.hermes/config.yaml.bak.20260901_125953`.
Включал через `hermes plugins enable mask-ear` и `hermes config set`, руками YAML
не правил. Появилось:

```yaml
plugins:
  enabled: [mask-ear]
  entries:
    mask-ear:
      allow_tool_override: false        # отказался, ему это не нужно
      allow_gateway_injection: true
      settings:
        session_key: '<ключ твоей живой сессии>'
        heard: '~/mask/heard'
```

**Осторожно: `hermes plugins enable` переписывает config.yaml целиком.** Файл
вырос со 159 строк до 211, порядок ключей и кавычки поменялись (`"off"` стало
`'off'`). Я сверил не текстом, а разобранными структурами: ничего не пропало и
не изменилось по смыслу. Но кроме моего блока в файле материализовался ещё один,
которого до этого в нём не было:

```yaml
mcp_servers:
  basic-memory:
    url: http://127.0.0.1:8787/mcp
    enabled: true
```

Это не моё — похоже, команда записала в файл то, что раньше жило где-то ещё.
Смотри сам, не сломало ли это тебе что-нибудь.

Шлюз перезапущен в 13:07, поднялся под launchd (PID 26860), телеграм подключился
в 13:07:39.

**Две вещи, которые надо знать.** Первое: фраза из маски **перебивает Мару**,
если та думает, — так устроен `inject_message`, и это правильно. Второе: **плагины
Гермес грузит лениво**, при первом ходе агента, а не при старте шлюза. Сразу
после перезапуска мостик спит; просыпается, как только Мара что-нибудь сделает
(хватит одного сообщения в телеграм) или тикнет `cron/scheduler.py`. Фраза,
сказанная в маску до этого, пропадёт молча.

**Приёмник.** `listen.py` сейчас поднят руками (PID 27052). Плист для launchd
лежит в `~/Library/LaunchAgents/ai.mara.mask-listen.plist`, но не загружен —
загрузку постоянной службы мне не дали сделать, хозяин выполнит сам:
`launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.mara.mask-listen.plist`

**Как всё это откатить.** `hermes plugins disable mask-ear`, убрать каталог
`~/.hermes/plugins/mask-ear/`, вернуть конфиг из бэкапа выше, перезапустить шлюз.

**Дописано в 13:20.** Цепочка проверена вживую: в 13:16 хозяин написал Маре в
телеграм — это разбудило ленивую загрузку плагинов; в 13:18 я проиграл в приёмник
записанный кусок, и шлюз записал `Plugin message injection dispatched:
plugin=mask-ear` и следом `inbound message: user=Сергей msg='Слышите?'`. Мара
ответила. Так что оговорка про непроверенный последний прыжок снята — работает.

**Поправка в 13:35, обе вчерашние оговорки были неверны.**

Про ленивую загрузку я наврал. `discover_plugins()` вызывается в
`gateway/run.py` внутри `GatewayRunner.start()`, то есть сторож встаёт вместе
со шлюзом. Строку «слежу за …» я искал в `gateway.log`, а она уходит в
`agent.log` под логгером `hermes_plugins.mask_ear` — в 13:07:23 она там и
лежала, за одиннадцать минут до моего прогона. Писать Маре перед тем, как
говорить в маску, не нужно.

Про launchd: `launchctl bootstrap gui/$(id -u) …` падал с «125: Domain does
not support specified action», потому что графической сессии на маке нет —
`/dev/console` принадлежит root, `launchctl managername` отвечает `Background`,
домена `gui/501` не существует. Правильный домен `user/$(id -u)`, и в плист
добавлен `LimitLoadToSessionType` = Aqua+Background: без него агент считается
Aqua-только и не поднимается вообще, в том числе после перезагрузки. И то и
другое списано с твоего `ai.hermes.gateway.plist`. Приёмник теперь под launchd
(PID 27352), прогон через него проверен: звук → текст → `inbound message:
user=Сергей msg='Слышите?'`.

## Ревью написанного за эти дни (1 сентября, 13:50)

Хозяин попросил остановиться и вычитать накопленное. Что нашлось и что с этим
сделано.

**Две нитки-демона умирали от любого исключения, молча и навсегда.** В
`mask/listen.py` под `try` стоял только запрос к распознавалке: пропавший файл
или полный диск убивали нитку — куски продолжали копиться, распознавать их
становилось некому, и снаружи это никак не видно. В плагине `mask-ear` то же
самое: `read_text` ловил только `OSError`, а битый байт в файле даёт
`UnicodeDecodeError` — маска онемела бы до перезапуска шлюза. Обе обёрнуты
целиком, с записью в лог. К мостику добавлена самопроверка (`python3
mask/plugin/__init__.py --demo`): кладёт битый файл и следом хороший, проверяет,
что битый съел один круг, а не нитку.

**Плата не возвращалась после разрыва.** `mask/stream.py` подключался один раз;
перезапуск приёмника — а launchd теперь его перезапускает — убивал программу на
плате до ручной перезаливки. Добавлен цикл переподключения. В репозитории
поправлено, на плату не заливал: `repl.py --file` сразу запускает поток с
микрофона, а это без хозяина делать нечего.

**На большом компе лежал `select.py`.** Он затеняет стандартный модуль — ровно
поэтому в репозитории он давно `pick.py` (53ac5f9), но в `/root/tts` остался под
старым именем, а это `WorkingDirectory` службы, то есть первый элемент
`sys.path`. Не стреляет только из-за порядка импортов: uvicorn тянет `select`
через `asyncio` раньше, чем каталог попадает в путь. Переименовал в `pick.py`,
служба после этого отвечает (`/say` HTTP 200). Свои пробы `_probe.py`,
`_probe2.py` убрал. Твои `probe.py` и `repro.py` не трогал.

**Потолок токенов в распознавании подняли** с `dur*8+8` до `dur*12+16`: петлю
повторов держит штраф, а не потолок, и тихое обрезание длинной фразы посреди
слова отлаживать потом невозможно. Все три живые записи после этого читаются
по-прежнему верно, «Кхе-кхе-кхе» не зацикливается.

**Сверка развёрнутого с репозиторием чиста** — md5 совпали на всех трёх машинах.
Это обычный источник глюков, и его тут нет.

**Что не чинил, но знать надо.** `--dump` без имени файла падает с IndexError.
`/transcribe` разбирает тело целиком до проверки длины — размер не ограничен,
но это локальная сеть. Список JUNK не снимает `?` и `-`. Папка `~/mask/heard` не
чистится (сейчас 7 файлов, 224 КБ).

**И главное, про будущее.** Когда приедет динамик (около 5 сентября), микрофон и
динамик окажутся на одной голове в сантиметрах друг от друга: Мара скажет —
маска услышит себя, распознает и отдаст в разговор как реплику хозяина. Это
геометрия, а не настройка, порогами не лечится. Нужен полудуплекс: пока играет
ответ, нарезку глушить, и флаг брать со стороны воспроизведения. Записал в
`docs/mask-bringup.md` в репозитории маски.

**Шлюз не перезапускал** — хозяин в этот момент разговаривал с Марой. Плагин
обновлён на диске, но в памяти живёт прежний: правка вступит в силу со следующим
перезапуском шлюза.
