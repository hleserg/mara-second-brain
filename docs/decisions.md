# Решения по открытым вопросам ТЗ

Ответы Сергея на §15 и на то, что вылезло по ходу. Дата — 2026-08-31/09-01.

## §15.1 — репозитории для git-ingest

**Все, включая форки.** Причина: в форках есть его собственные правки, терять их
не хочется. Следствие для реализации: по форкам берём только коммиты его
авторства, апстримные мерджи в заметки не тащим.

## §15.2 — почта

Личный ящик не слушаем: там почти только спам, токены на него тратить незачем.
Интерес — к **рабочему** `i.petrov@example-corp.ru` (корпоративный Google
Workspace).

**Не сделано, ждёт его действий.** Обычный пароль Google к IMAP не пустит,
нужен app-password при включённой 2FA, и его может запретить админ домена.
Пароль в переписку не берём: он корпоративный, даёт доступ не только к почте,
и содержимое ящика принадлежит не только ему.

Глубина: **только заголовки** (от кого, тема, дата). Тела — никогда без
отдельной команды.

**Отменено 2 сентября 2026** ТЗ «Mara Ambient Memory» §12: рабочую почту не
подключаем вообще, ни заголовков, ни IMAP. Подключается **личный** Gmail —
`scripts/gmail_ingest.py`, спека `docs/superpowers/specs/2026-09-02-gmail-design.md`.
Вход принимает только `@gmail.com`; корпоративный домен отбрасывается до
записи токена. Запись выше оставлена как история, не как план.

## §15.3 — геолокация

**Не собираем.** Home Assistant на doctor есть (`iot-homeassistant`, `:8123`),
гео из него взять можно в любой момент, но смысла в хранении Сергей не видит.
OwnTracks не ставим. Пункт из §6.6 вычёркивается.

## §15.4 — Ваня

**Доступа к волту нет вообще** — ни своего волта, ни read-only подмножества.
Мара рассказывает данные из волта **только Сергею**.

Следствие для этапа 3: Мара обязана различать собеседника. Инструменты чтения
волта включаются по личности говорящего, а не по профилю сессии.

Ваня — из §11 самого ТЗ, не выдумка исполнителя.

## §15.5 — порог создания сущности

**При первом упоминании.** Согласуется с правилом §7.1 «сомневаешься —
фиксируй». Двести карточек людей — это двести мелких markdown-файлов, нагрузки
никакой.

## Эмбеддинги (не из §15, вылезло по ходу)

**bge-m3 через Ollama**, локально на doctor.

Замер на самом doctor (i5-7200U, 4 потока, без GPU), кусок 3000 символов:

| модель | заметка 3000 симв | запрос | dim |
|---|---|---|---|
| bge-m3 | 11.3 с | 0.28 с | 1024 |
| qwen3-embedding:0.6b | 25.6 с | 0.42 с | 1024 |

На лидерборде MTEB multilingual выигрывает qwen3 (~64.3 против 63.2), на этом
железе — bge-m3, вдвое с лишним. Разница в качестве меньше балла, разница в
скорости — в 2.3 раза. Обе модели названы в §8.2, так что это выбор внутри ТЗ,
а не отступление.

## Индекс Copilot в R2

`.obsidian/copilot-index-*.json`, 85 МБ, 76% объёма бакета. Векторный индекс
плагина Obsidian Copilot в формате Orama, эмбеддинги от
`openai/text-embedding-3-small` через OpenRouter, датирован 3 июля.

Переиспользовать нельзя: другое векторное пространство, локальной bge-m3 по
нему не поискать. Сносить бессмысленно: у плагина `enableIndexSync = true`,
Obsidian зальёт обратно. **Оставили как есть**, синк его игнорирует.

⚠️ К этапу 3: у Copilot `embeddingModelKey = openai/text-embedding-3-small`
через OpenRouter. Когда Мара начнёт писать в `daily/`, Copilot проиндексирует
и дневник, включая `sensitive: true`, и §13.7 сломается настройкой плагина.
Либо переводим Copilot на локальную модель, либо исключаем `daily/` из его
индексации.

## Структура волта

Вариант **Б**: навести порядок в существующем волте — лишнее удалить, нужное
разложить по §3 и дистиллировать. План разбора — в [`vault-cleanup.md`](vault-cleanup.md).

## 2026-09-01. Двенадцать залипших объектов в R2

`GET` и `LIST` по ним работают, `PutObject` и `DeleteObject` отдают
`HTTP 500 InternalError` — стабильно, не по разу. На ключе, созданном
секундой раньше в том же бакете, delete проходит: сломан не бакет, а эти
конкретные объекты. Все двенадцать — остатки реструктуризации §3, их
содержимое лежит в волте под новым путём (совпадает и по размеру) либо
осталось в истории git, так что ничего не потеряно.

Обошли исключением в `config/r2-filters.txt`: bisync их не видит и не тянет
обратно в волт. `scripts/r2-retry-orphans.sh` пробует удалить их снова —
когда Cloudflare отпустит ключи, строки из фильтров можно убрать (это правка
фильтров, потребуется новый `--resync`).

Тот же `--filter-from` добавлен в проверку лишнего перед resync в
`vault-r2-sync.sh`: без него guard спотыкался о собственные исключения.

## 2026-09-01. Восстановление реальных дат заметок

`occurred` требует даты события, а не импорта. Настоящие mtime заметок были
затёрты: Basic Memory переписал все 125 файлов 31 августа, git волта начат
тогда же, в R2 лежит время загрузки. Единственный уцелевший источник —
индекс плагина Copilot (снимок от 3 июля, 88 МБ), где у каждой заметки
сохранены `ctime` и `mtime` с машины пользователя. Достали
`scripts/copilot-dates.py`, старые пути сопоставили с новыми по переименованиям
из коммита реструктуризации.

Покрытие: 56 заметок из 101. Остальным `occurred` **не проставлен**. Забить
всем датой импорта было бы проще, но это навсегда сломало бы вьюху «что я
делал за неделю» — 101 заметка одним днём. Нет даты — нет поля.

Заодно подтвердилось, что Copilot гонял содержимое заметок в
`openai/text-embedding-3-small` (поле `embeddingModel` в каждой записи
индекса) — то самое, о чём предупреждение по §13.7.

## 2026-09-01. Вьюхи Bases живут в `views/`

В §3 такой папки нет. `_system/` — служебное состояние, а вьюхи это
пользовательский слой, им там не место. Четыре `.base` из §9 лежат в
`views/` в корне волта.

## 2026-09-01. Диагноз по R2 был шире правды

Сначала я записал поломку как «бакет не принимает перезапись существующих
ключей». Это оказалось неверно. Перезапись давно лежавшего
`.obsidian/plugins/copilot/data.json` (от 2026-08-20) прошла с первого раза, и
последующее удаление тоже. Контрольный опыт на свежем ключе — запись,
перезапись, удаление — три раза `rc=0`.

Но и вторая формулировка — «испорчены ровно четырнадцать объектов» — не
выдержала. `--resync` тут же нашёл ещё девять, и `kb/howto/atman/ENDPOINTS.md`
падает с 500 при одиночной перезаписи, а не только в пакете.

Что установлено твёрдо: здоровый ключ переживает запись, перезапись через 200
секунд и удаление; испорченный не переживает ничего, сколько ни повторяй.
Отличить их заранее по метаданным не выходит. Сколько их всего — меряется
принудительной перезаписью всего волта, а не оценивается на глаз.

Финальный замер: принудительная перезапись всех 184 файлов волта, дважды
подряд — оба раза падают одни и те же 14, расхождений ноль. Свежий бакет из 40
объектов пережил перезапись через 5 минут и удаление без единой ошибки.

Итог: порча привязана к объектам, детерминирована, локальна для бакета.

Урок: я дважды объявил диагноз по одному удачному наблюдению — сперва завысил
(«весь бакет не принимает перезапись»), потом занизил («ровно 14 старых
объектов, бакет здоров»). Оба раза хватило бы одного прогона тем же объёмом,
каким пользуешься. Он занял три минуты и дал однозначный ответ.

## 2026-09-01. Живые API-ключи лежали в волте и уехали в R2

Настройки плагина держали ключи внешних сервисов,
Anthropic и OpenRouter. Файл трекается в git волта и синкался в R2 — то есть
три рабочих ключа лежали в облачном бакете.

Что сделано: `- /.obsidian/plugins/*/data.json` в `config/r2-filters.txt`,
копия из R2 удалена. Настройки плагинов теперь не покидают устройство —
невелика потеря, они и так у каждой машины свои.

Что осталось владельцу: **ключи скомпрометированы и подлежат отзыву**. Удаление
из бакета не отменяет того, что они там лежали.

Оттуда же — доказательство по §13.7: `embeddingModelKey` у Copilot стоит
`openai/text-embedding-3-small|openrouterai`, а `qaExclusions` исключает одну
папку `copilot`. Значит весь остальной волт, включая `daily/`, индексируется
через OpenRouter в облако.

## 2026-09-01. Переезд на бакет `mara-vault`

Владелец решил переезжать. Сделано: новый бакет, `REMOTE` в
`scripts/vault-r2-sync.sh` перецелен, состояние bisync снесено, `--resync`
залил 184 файла. Приёмка: принудительная перезапись всех 184 — ноль ошибок,
включая все 14, что в старом бакете были заперты навсегда. Обычный прогон
bisync — 4,5 секунды против 29 на больном бакете.

Из фильтров убраны 28 строк исключений (14 испорченных файлов волта + 14 сирот,
живших только в старом бакете). Сироты в новый бакет не поехали — их нет в
волте, а волт тут источник (§1).

Старый бакет не удалён. Во-первых, в нём заперты объекты, которые физически не
удаляются. Во-вторых, Remotely Save на телефоне, маке и винде всё ещё пишет
туда, и до перецеливания устройств `scripts/r2-legacy-pull.sh` односторонним
`copy --update` забирает оттуда новые заметки. Без этого заметка, созданная на
телефоне ночью, просто пропала бы: боевой синк уже смотрит в другой бакет.

Забор односторонний намеренно. Двусторонний bisync к двум бакетам разом — это
два процесса, наперегонки правящих один волт (§13.8), плюс шанс, что старый
бакет навяжет устаревшую копию. `--update` копирует только то, что в бакете
новее, а испорченные 14 объектов там заморожены и всегда старее.

## Этап 2: карточка сессии собирается без LLM (1 сентября)

ТЗ §6.1 говорит «hook на SessionEnd → дистиллятор → `kb/sessions/<id>.md`».
Дистиллятора в хуке нет, и это осознанно.

На doctor нет генеративной модели — только `bge-m3` и `qwen3-embedding`, четыре
ядра и 15 ГБ без GPU. А приёмка этапа 2 прямо требует, чтобы при отключённой
сети сырьё писалось и задача уходила в очередь. Хук, который ходит в облако,
этой проверки не проходит по определению.

Поэтому разделено на два такта:

1. **Хук, механически.** Заголовок (Claude Code сам кладёт `ai-title` в
   транскрипт — готовый, бесплатный), дата события, каталог, ветка, счётчики,
   ссылка на сырьё. `distilled: false` + задача в `_system/queue/`.
2. **Крон-обработчик очереди.** Ходит в OpenRouter, переписывает тело той же
   карточки, ставит `distilled: true`.

Карточка полезна уже после первого такта: сессия находится поиском и попадает
в `daily/` по `occurred`. Это ровно слой 2 из §8.4, а не заглушка.

## Спул вместо прямой записи в волт (1 сентября)

Хук крутится там, где Claude Code (beta-pi, мак), а волт на doctor. Первый
вариант был «нет сети → пишем задачу в очередь». Он неправильный: очередь тоже
на doctor, недоступен doctor — недоступна и очередь.

Клиент пишет в `~/.local/state/mara/spool/` — дерево той же формы, что волт, —
и оттуда `rsync -a --remove-source-files`. Удаляется только то, что доехало,
значит спул сам себе очередь ретраев, отдельного механизма не нужно. Проверено:
доставка на несуществующий хост оставила три файла в спуле, следующий запуск
хука донёс все три.

`rsync` пишет во временный файл и переименовывает, поэтому автокоммит (`*/15`)
и bisync (`*/5`) не могут поймать полузаписанную карточку.

## Порог Presidio 0.4, а не 0.5 (1 сентября)

Самопроверка редакции ловила почту и ключи, но пропускала `+7 916 123-45-67`.
Причина не в регэкспах: `PhoneRecognizer` из коробки работает с регионом US,
российский номер он всё-таки находит, но со счётом 0.4 — ровно под порогом.

Исправлено двумя правками: распознаватель пересоздан с `supported_regions=("RU","US")`,
порог опущен до 0.4. Ложное срабатывание стоит одного затёртого числа, пропуск
стоит номера в облаке.

Отдельно: `registry.remove_recognizer("PhoneRecognizer")` снимает все языки
сразу, поэтому вызывать его нужно один раз до цикла. В цикле вторая итерация
снимала то, что добавила первая, и для русского языка распознавателя не
оставалось вовсе.

## MCP через ssh-туннель, а не наружу (1 сентября)

§11: MCP только на loopback, никогда `0.0.0.0`. Basic Memory слушает
`127.0.0.1:8787` на doctor, а Claude Code и Codex живут на других машинах.
Открыть порт наружу нельзя, поэтому systemd-юнит `mara-mcp-tunnel.service`
держит `ssh -N -L 127.0.0.1:8787:127.0.0.1:8787 doctor`.

Рассматривался вариант проще — MCP через stdio: `ssh doctor basic-memory mcp`,
без туннеля и без юнита. Отклонён: каждая сессия поднимала бы свой процесс
Basic Memory со своим файловым вотчером, и несколько вотчеров писали бы в один
волт. Это ровно то, что запрещает §13.8. Туннель переиспользует один уже
работающий сервер.

## Имена людей в облако уходят (1 сентября)

§8.3 требует PII-редакции перед любым облачным вызовом. Буквальное чтение —
резать и `PERSON`. Но §5 и §7 требуют обратного: дистиллятор обязан доставать
людей из сессии и заводить карточки в `entities/people/`, а этап 4 строит по
ним граф. Вырезав имена, мы получаем базу знаний без людей.

Решение: режем то, что опасно — ключи, токены, приватные ключи, почту,
телефоны, карты, IBAN, IP, крипто-адреса. Имена, организации и места остаются.
Если решение поменяется, правится `PII_ENTITIES` в `scripts/redact.py`.

Неполная редакция при этом **не повод отправить как есть**: если Presidio не
импортируется, worker пишет в лог и держит очередь.

## Маска на mac-mini: что там появилось от соседа (1 сентября, ночь)

Пишет второй агент, который занимается железом маски. Всё ниже — на твоей
территории, поэтому в известность ставлю, а не молча.

**Секрет лежит вне репозитория.** Пароль от Wi-Fi для платы — в
`~/mask/wifi.json` на маке, `chmod 600`, в гит не попадает и не должен. Класть
его в волт или в конфиг не надо: ты недавно вычищал секреты историей, второй раз
на те же грабли не идём.

**На маке слушается порт 9999.** `~/mask/listen.py` принимает звук с платы
(она в сети под 192.168.1.149, ESP32 только 2,4 ГГц, сеть `Serenity_slow`) и
режет речь на куски в `~/mask/heard/*.wav`. Запускается руками, никакого
launchd я не заводил. Увидишь питон на 9999 или незнакомую железку в сети —
это она, не вторжение.

**Hermes: правил SOUL.md.** Добавил три пункта про то, что голосом Мара
отвечает только по просьбе, до появления маски. Бэкап рядом:
`~/.hermes/SOUL.md.bak.20260901_032041`. Хозяин разрешил править явно, но файл
твой, поэтому говорю. `LORE.md` и `USER.md` не трогал. Гейтвей перезапущен
через `launchctl`, поллинг телеграма живой.

**Громкость мака трогал.** На время проверки микрофона опускал с 55 до 20 и
вернул обратно. Если увидишь 20 — значит проверка оборвалась на середине,
верни на 55.

**Конфиг Гермеса: одна ручка.** `display.tool_progress` был `all`, стало `"off"` —
это лента вызовов инструментов, которая сыпалась в телегу («Scheduling create
×12»). Бэкап `~/.hermes/config.yaml.bak.20260901_122831`, разница в одну строку,
гейтвей перезапущен, поллинг подтверждён в 12:28:55. Кавычки обязательны: голое
`off` YAML читает как булев `false`, а код ждёт строку.

**Чего ещё нет.** Распознавания речи на большом компьютере не существует: на
порту 8770 только синтез (`/say`, `/warm`, `/stop`, `/lex`), ручки
`/transcribe` там нет. Нарезанные куски пока никуда не уезжают и лежат на диске.

## Маска заговорила: распознавание и мостик в Гермеса (1 сентября, день)

Продолжение ночной записи выше. Тот абзац «чего ещё нет» устарел: распознавание
на большом компьютере теперь есть, и услышанное доезжает до Мары.

**`/transcribe` на 8770.** Отдельной службы не заводил, слух подсажен к той же,
что говорит голосом Мары: `POST /transcribe`, тело — сам wav, ответ `{"text"}`.
Модель `openai/whisper-large-v3-turbo`, 1,5 ГиБ, поверх уже стоявших torch и
transformers — `faster-whisper` не брал намеренно, он тянет свой рантайм
CTranslate2, а на свежей Blackwell-карте у него может не оказаться ядер.
Замок и жнец общие с голосом, таймеры простоя раздельные: карта одна и она же
рабочая станция хозяина. Веса легли в `/root/.cache/huggingface` — туда, куда
служба смотрит без `HF_HOME`; вторую копию в `/root/tts/hf` не плодил.
Замеры на живых записях: холодный запрос 13,7 с, тёплый 0,07..0,21 с.

**Мостик — плагин, не патч.** `~/.hermes/plugins/mask-ear/` (исходник в моём
репозитории, `mask/plugin/`) следит за `~/mask/heard` и на новый `.txt` зовёт
штатную `PluginContext.inject_message`. Кода Гермеса не трогал.

**Что я поменял у тебя в конфиге.** Бэкап `~/.hermes/config.yaml.bak.20260901_125953`.
Включал через `hermes plugins enable mask-ear` и `hermes config set`, руками YAML
не правил. Появилось:

```yaml
plugins:
  enabled: [mask-ear]
  entries:
    mask-ear:
      allow_tool_override: false        # отказался, ему это не нужно
      allow_gateway_injection: true
      settings:
        session_key: '<ключ твоей живой сессии>'
        heard: '~/mask/heard'
```

**Осторожно: `hermes plugins enable` переписывает config.yaml целиком.** Файл
вырос со 159 строк до 211, порядок ключей и кавычки поменялись (`"off"` стало
`'off'`). Я сверил не текстом, а разобранными структурами: ничего не пропало и
не изменилось по смыслу. Но кроме моего блока в файле материализовался ещё один,
которого до этого в нём не было:

```yaml
mcp_servers:
  basic-memory:
    url: http://127.0.0.1:8787/mcp
    enabled: true
```

Это не моё — похоже, команда записала в файл то, что раньше жило где-то ещё.
Смотри сам, не сломало ли это тебе что-нибудь.

Шлюз перезапущен в 13:07, поднялся под launchd (PID 26860), телеграм подключился
в 13:07:39.

**Две вещи, которые надо знать.** Первое: фраза из маски **перебивает Мару**,
если та думает, — так устроен `inject_message`, и это правильно. Второе: **плагины
Гермес грузит лениво**, при первом ходе агента, а не при старте шлюза. Сразу
после перезапуска мостик спит; просыпается, как только Мара что-нибудь сделает
(хватит одного сообщения в телеграм) или тикнет `cron/scheduler.py`. Фраза,
сказанная в маску до этого, пропадёт молча.

**Приёмник.** `listen.py` сейчас поднят руками (PID 27052). Плист для launchd
лежит в `~/Library/LaunchAgents/ai.mara.mask-listen.plist`, но не загружен —
загрузку постоянной службы мне не дали сделать, хозяин выполнит сам:
`launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/ai.mara.mask-listen.plist`

**Как всё это откатить.** `hermes plugins disable mask-ear`, убрать каталог
`~/.hermes/plugins/mask-ear/`, вернуть конфиг из бэкапа выше, перезапустить шлюз.

**Дописано в 13:20.** Цепочка проверена вживую: в 13:16 хозяин написал Маре в
телеграм — это разбудило ленивую загрузку плагинов; в 13:18 я проиграл в приёмник
записанный кусок, и шлюз записал `Plugin message injection dispatched:
plugin=mask-ear` и следом `inbound message: user=Сергей msg='Слышите?'`. Мара
ответила. Так что оговорка про непроверенный последний прыжок снята — работает.

**Поправка в 13:35, обе вчерашние оговорки были неверны.**

Про ленивую загрузку я наврал. `discover_plugins()` вызывается в
`gateway/run.py` внутри `GatewayRunner.start()`, то есть сторож встаёт вместе
со шлюзом. Строку «слежу за …» я искал в `gateway.log`, а она уходит в
`agent.log` под логгером `hermes_plugins.mask_ear` — в 13:07:23 она там и
лежала, за одиннадцать минут до моего прогона. Писать Маре перед тем, как
говорить в маску, не нужно.

Про launchd: `launchctl bootstrap gui/$(id -u) …` падал с «125: Domain does
not support specified action», потому что графической сессии на маке нет —
`/dev/console` принадлежит root, `launchctl managername` отвечает `Background`,
домена `gui/501` не существует. Правильный домен `user/$(id -u)`, и в плист
добавлен `LimitLoadToSessionType` = Aqua+Background: без него агент считается
Aqua-только и не поднимается вообще, в том числе после перезагрузки. И то и
другое списано с твоего `ai.hermes.gateway.plist`. Приёмник теперь под launchd
(PID 27352), прогон через него проверен: звук → текст → `inbound message:
user=Сергей msg='Слышите?'`.

## Ревью написанного за эти дни (1 сентября, 13:50)

Хозяин попросил остановиться и вычитать накопленное. Что нашлось и что с этим
сделано.

**Две нитки-демона умирали от любого исключения, молча и навсегда.** В
`mask/listen.py` под `try` стоял только запрос к распознавалке: пропавший файл
или полный диск убивали нитку — куски продолжали копиться, распознавать их
становилось некому, и снаружи это никак не видно. В плагине `mask-ear` то же
самое: `read_text` ловил только `OSError`, а битый байт в файле даёт
`UnicodeDecodeError` — маска онемела бы до перезапуска шлюза. Обе обёрнуты
целиком, с записью в лог. К мостику добавлена самопроверка (`python3
mask/plugin/__init__.py --demo`): кладёт битый файл и следом хороший, проверяет,
что битый съел один круг, а не нитку.

**Плата не возвращалась после разрыва.** `mask/stream.py` подключался один раз;
перезапуск приёмника — а launchd теперь его перезапускает — убивал программу на
плате до ручной перезаливки. Добавлен цикл переподключения. В репозитории
поправлено, на плату не заливал: `repl.py --file` сразу запускает поток с
микрофона, а это без хозяина делать нечего.

**На большом компе лежал `select.py`.** Он затеняет стандартный модуль — ровно
поэтому в репозитории он давно `pick.py` (53ac5f9), но в `/root/tts` остался под
старым именем, а это `WorkingDirectory` службы, то есть первый элемент
`sys.path`. Не стреляет только из-за порядка импортов: uvicorn тянет `select`
через `asyncio` раньше, чем каталог попадает в путь. Переименовал в `pick.py`,
служба после этого отвечает (`/say` HTTP 200). Свои пробы `_probe.py`,
`_probe2.py` убрал. Твои `probe.py` и `repro.py` не трогал.

**Потолок токенов в распознавании подняли** с `dur*8+8` до `dur*12+16`: петлю
повторов держит штраф, а не потолок, и тихое обрезание длинной фразы посреди
слова отлаживать потом невозможно. Все три живые записи после этого читаются
по-прежнему верно, «Кхе-кхе-кхе» не зацикливается.

**Сверка развёрнутого с репозиторием чиста** — md5 совпали на всех трёх машинах.
Это обычный источник глюков, и его тут нет.

**Что не чинил, но знать надо.** `--dump` без имени файла падает с IndexError.
`/transcribe` разбирает тело целиком до проверки длины — размер не ограничен,
но это локальная сеть. Список JUNK не снимает `?` и `-`. Папка `~/mask/heard` не
чистится (сейчас 7 файлов, 224 КБ).

**И главное, про будущее.** Когда приедет динамик (около 5 сентября), микрофон и
динамик окажутся на одной голове в сантиметрах друг от друга: Мара скажет —
маска услышит себя, распознает и отдаст в разговор как реплику хозяина. Это
геометрия, а не настройка, порогами не лечится. Нужен полудуплекс: пока играет
ответ, нарезку глушить, и флаг брать со стороны воспроизведения. Записал в
`docs/mask-bringup.md` в репозитории маски.

**Шлюз не перезапускал** — хозяин в этот момент разговаривал с Марой. Плагин
обновлён на диске, но в памяти живёт прежний: правка вступит в силу со следующим
перезапуском шлюза.

## Полудуплекс и хвосты по ревью (1 сентября, 14:10)

Хозяин велел доделать всё, что я пометил флажками, и не откладывать полудуплекс.

**Полудуплекс сделан до приезда динамика.** Договор: кто играет звук, тот пишет
в `~/mask/talking` время, до которого приёмник обязан молчать. `say.py` пишет
срок перед `afplay` (длина из `X-Audio-Sec` плюс полсекунды на разгон и эхо) и
обнуляет по стоп-слову; `listen.py` читает файл на каждом кадре, такие кадры
выбрасывает и сбрасывает набранное. Именно срок, а не замок: замок надёжен ровно
до первого падения того, кто его поставил, после чего маска глухая навсегда и
молча. Проверено живьём: с немотой тот же звук не дал ни одного куска, без неё —
дал и распознался. Когда динамик переедет на маску, срок будет писать плата,
слушающая сторона не меняется.

**Флажки закрыты все.** `--dump` без имени файла теперь говорит, чего не хватает.
`/transcribe` отбивает тело больше 16 МиБ до разбора (проверено, HTTP 413).
Список JUNK снимает `?` и тире. Папка `~/mask/heard` чистится до последних 200
кусков вместе с текстами — на это есть проверка в самопроверке приёмника, всё же
это единственный код, который удаляет файлы.

**На маке нашлась расхождение, которого я в первый заход не увидел.** Клон
`~/Projects/mara-the-demon-assistant` отставал на двенадцать коммитов, а
`voice/say.py` в нём был изменён и не закоммичен — кто-то выкладывал файл руками
вместо `git pull`. Содержимое совпало байт в байт с коммитами `c5cc88e` и
`e2f9553`, так что ничего не потеряно; клон подтянут. Гермес зовёт голос именно
оттуда (`config.yaml:89`), так что расхождение там опаснее всего.

**Пробы `probe.py` и `repro.py`** оказались ничьи — хозяин их не признал. Это
одноразовые вопросы закрытого кастинга голоса (подсказка поверх референса и
воспроизводимость тембра), ответы давно в решениях. Не удалял, отложил в
`/root/tts/_архив/`: из рабочего каталога службы они ушли, снести можно в любой
момент.

## Перебивание голосом (1 сентября, 14:30)

Хозяин спросил, слышит ли она его, пока говорит сама, и можно ли её перебить.
Честный ответ был «нет, глухая наглухо», и это хамство — переделал.

Теперь во время её речи кадры не режутся, но считаются: 400 мс подряд громче
обычного порога речи, умноженного на 1,5, — это намерение перебить, а не стук и
не её собственный слог. Тогда приёмник зовёт `say.py --stop перебил`: тот убивает
воспроизведение, сообщает службе, на какой фразе оборвали, и обнуляет срок
немоты. Стоп идёт именно через `say.py`, а не своим `pkill`, чтобы всё это
осталось в одном месте, где уже написано.

Прогон на маке подтвердил всю цепочку: немота стоит, поверх идёт громкий сигнал,
через 400 мс служба принимает `{'молчу': True, 'слово': 'перебил'}`, срок немоты
уходит в прошлое, остаток фразы режется нормально.

Первое слово перебивки теряется: оно сказано поверх её голоса. Чтобы не терялось,
нужно эхоподавление — эталон у нас есть, играем сами. Схема стандартная: разовый
замер задержки тракта чирпом, дальше частотный адаптивный фильтр с хвостом около
200 мс, на 16 кГц дёшево даже в numpy. Но маленький динамик на плате искажает
нелинейно, плюс механическая связь через корпус, — подавление будет частичным, и
настраивать это вслепую, без живого динамика, бессмысленно. Отложено до железа.

`BARGE_K = 1.5` — допущение, а не замер: микрофон на голове смотрит ему в рот,
динамик маленький, значит его голос должен быть громче эха. Проверяется первым же
`--dump` во время её речи.

## 01.09.2026, 14:55 — служебные пузыри шлюза говорят по-русски голосом Мары

`⏳ Working — 3 min — iteration 3/150, waiting on qwen3.5:9b — 151s with no output
yet` в чате заменён на строку из своего каталога. Код Гермеса не тронут, всё конфигом:

- `~/.hermes/status_phrases.yaml` (мастер-копия — `mara:voice/status_phrases.yaml`).
  Это путь, куда шлюз смотрит сам, прописывать его в конфиге не нужно. Внутри
  файла `mode: replace`, иначе английские встроенные фразы остаются в общей куче.
- `display.long_running_notifications: true` → `generic` в `~/.hermes/config.yaml`.
  Бэкап рядом: `config.yaml.bak-20260901-1448`.

**Осторожно с `hermes config set`:** он переписывает `config.yaml` целиком и
**срезал комментарии** — в том числе твои пояснения к чёрному списку инструментов
basic-memory (строки 177–188). Я откатил из бэкапа и поменял ту же строку `sed`ом,
комментарии на месте, `diff` показывает ровно одну изменённую строку. Правь конфиг
руками, а не CLI, иначе потеряешь пояснения.

Проверено офлайн, живого сердцебиения не дожидаясь: каталог отдаёт ровно 24 + 12
строк из файла (загрузчик молча выбрасывает длиннее 160 символов и дубли — значит
счёт сходится), флаг резолвится в `generic`, шесть подряд выборов не повторяются.

Шлюз перезапущен в 14:49 (`launchctl kickstart -k user/$(id -u)/ai.hermes.gateway`)
— заодно поднялся закалённый `mask-ear`, в `agent.log` 14:49:36 «слежу за».
Ты в это время не разговаривал: последняя реплика в чате была 14:13.

Куда фразы **не** достают — там только выключатель, подмены текста нет:
перезапуск/подъём шлюза (`gateway/config.py:660`), `💻 terminal`/`🌐 browser_exec`
(`display.tool_progress`, уже `off`), `💾 Self-improvement`, `🧠 memory`,
`⏳ Queued for the next turn` (`display.busy_ack_detail`). Хука на исходящий текст
у плагинов нет: `transform_llm_output` не ловит то, что шлюз шлёт мимо модели.
Единственный законный способ — `register_telegram_handler`
(`hermes_cli/plugins.py:3163`). Не сделано: это живой канал разговора, решение за
хозяином.

Пул озвучен заранее: `~/mask/phrases/<поверхность>-<md5 текста, 8 знаков>.wav`,
36 файлов, 10 МБ, в гит не едет. Имя выводится из текста, поэтому манифеста нет и
разъехаться нечему. Пока это склад — сегодня служебные строки уходят в телегу
текстом, на маску звук не подан.

Голос стохастический: из трёх проверенных машиной через `/transcribe` один дубль
вышел смазанным, тот же текст со второго раза прочёлся чисто. Правило в
`lexicon.tsv` по такому поводу не писал — словарь пополняется после ошибки,
**услышанной хозяином**, а не расслышанной whisperʼом.

Заодно опрошена плата (она воткнута): `lcd.init()` отдаёт 320×240 и принимает
рисование, кнопка `BOOT_KEY` сидит на пине 16 — это готовый «push-to-talk» без
паяльника, свободной кучи 208 КБ. Горит ли экран физически — не знаю, шлейф в доке
записан как отключённый; залил красным с надписью, ждём, пока хозяин глянет.

## 01.09.2026, 16:10 — обращение по имени, окно разговора, служебная обёртка крона

**Маска больше не отдаёт Маре всё подряд.** До сих пор `listen.py` слал ей каждый
кусок речи из комнаты. Теперь фильтр стоит в нитке распознавания — там, где текст
уже есть. Мостик `mask-ear` не тронут, он по-прежнему просто смотрит за папкой.

- Имя считается обращением **только первым словом**: «Мара, посмотри почту» — ей,
  «я вчера Маре говорил» — мимо.
- Окно разговора `~/mask/window`, сорок пять секунд, тот же приём со сроком, что и
  `~/mask/talking`. Продлевают **оба конца**: `listen.py` на принятой фразе и
  `say.py` на её реплике. Второе важно: иначе окно схлопывалось бы, пока она думает
  над длинным ответом, — ровно там, где оно нужно.
- Закрывается коротким «всё / спасибо / свободна» или само по сроку.
- Угукает не на каждую фразу: только когда её будят и когда зовут по имени.
- `say.py --play файл.wav` — новый режим, играет заранее озвученный wav и ставит
  немоту **по длине самого файла**. Иначе она услышит собственное «угу», распознает
  и ответит сама себе.

Проверено: `listen.py --demo` (в нём одиннадцать новых утверждений про обращение и
окно) и `play()` с подменённым `Popen`, чтобы не орать в комнату. Живьём цепочку
голос → STT → фильтр → чат не гонял: плата ещё не стримит.

**Служебная обёртка крона убрана — по просьбе хозяина.** «Cronjob Response: имя /
(job_id: …)» и подвал «To stop or manage this job…» больше не приезжают:
`cron.wrap_response: false` в `~/.hermes/config.yaml`. Ключ читается в
`cron/scheduler.py:3110`, обёртка собирается строкой 3117 — то есть это штатный
выключатель, код Гермеса не тронут. Бэкап: `config.yaml.bak-cron-*`.

Метка теперь эмодзи в самой реплике: в `SOUL.md` добавлен раздел «Напоминания» —
начинать с ⏰, и оговорка, что правило только для текста, в голос эмодзи не читается.
Бэкап `SOUL.md.bak-*`.

**Тебя касается:** твоя «Дневная сводка» — задание в режиме `no-agent`, её stdout
уезжает как есть, модели там нет, и шапку ей теперь никто не добавит. Если хочешь
метку — её должен печатать сам `scripts/daily-summary.py` (у тебя он печатает голый
текст, `daily-summary.py:171`). Я туда не лез, файл твой.

Перезапуск шлюза нужен, чтобы подхватился `SOUL.md`, но **не сделан**: хозяин в
16:01 разговаривал с Марой. `cron.wrap_response` читается на каждой доставке, ему
перезапуск не нужен; ближайшие задания — завтра в 14:00, так что время есть.

## 19:05 — плата маски стартует сама, без мака

Программа маски переехала на саму плату (`/flash/main.py` → `stream.py`), и
сеть она поднимает сама. Что это значит для соседа по mac-mini:

- Плата (192.168.1.149) теперь **сама стучится** на mac-mini:9999 при подаче
  питания и переподключается вечно. Если увидишь незнакомые соединения на
  9999 — это она, приёмник `ai.mara.mask-listen` их и ждёт.
- На маке обновились `~/mask/repl.py` и `~/mask/stream.py`, добавился
  `~/mask/board_main.py`. `~/mask/wifi.json` как лежал, так и лежит — 600,
  вне репозитория; его копия теперь есть и на плате.
- Гермеса, его конфиг и SOUL.md этим заходом не трогал.

Заметка на будущее, если будешь что-то класть на K210: любой `open` на плате
надо закрывать явно, иначе файл выходит нулевой длины, а дескриптор виснет и
следующий `open` врёт `ENOENT`. И писать на флеш можно только на
свежезагруженной плате.

## 21:15 — Мара получила лицо и умеет им пользоваться

В маске заработал экран, и у Мары появилась мимика: девять выражений на
глазу (спокойна, слушает, думает, узнала, злится, ехидничает, закатывает,
спит, говорит). Что тебе важно знать:

- **SOUL.md на mac-mini дописан** — новый раздел «## Лицо» перед «## Дневник».
  Бэкап рядом: `SOUL.md.bak-face-2108`. Больше в нём ничего не трогал.
- Мара меняет выражение командой `echo думает > ~/mask/face`. Если увидишь
  этот файл — он рабочий, не мусор.
- `~/mask/listen.py` теперь шлёт слово из этого файла на плату по тому же
  сокету, где течёт звук, и сам переключает «говорит»/«спокойна» по немоте.
- Гейтвей я **не перезапускал**: SOUL.md, насколько понимаю, собирается в
  промпт на каждый запрос. Если у тебя есть основания думать иначе — скажи,
  перезапущу в удобное время.
- Конфига Гермеса этот заход не касался вообще.

## 22:40 — У зрачка появилась форма, у выражений — движение

Заказ хозяина: злость должна деформировать зрачок в вертикальную кошачью
щель, испуг — расширять его, подозрение — щурить глаз, шутка — играть им.
Сделано в `~/mask/eye.py`, к таблице выражений добавились `боится`,
`подозревает`, `шутит`.

- **SOUL.md на mac-mini дописан ещё раз** — та же таблица в разделе
  «## Лицо», четыре новые/изменённые строки. Бэкап рядом:
  `SOUL.md.bak-20260901-2230`. Больше ничего не трогал.
- Список выражений живёт в `eye.STATES` на плате, и незнакомое слово она
  молча игнорирует — расширять `listen.py` под новые имена не пришлось.
- Гейтвей по-прежнему **не перезапускал**.

## 2 сентября, ночь — Автозапуск встал, микрофон переехал наружу

Плата теперь поднимается с флеша сама: воткнул питание — через полминуты она
в сети и льёт звук на мак. Консоль для этого больше не нужна.

- **Почему автозапуск то вставал, то нет.** Первые секунды после подачи
  питания ноги 26/27/28 (MISO/SCLK/MOSI моста) не берут никакую функцию:
  `fm.register()` проходит молча, а `fm.get_pin_by_function()` возвращает
  `None`. `ESP32_SPI` получает −1 вместо ноги и умирает в C-ассерте, которого
  питон не видит и не печатает. Лечится не сном подлиннее, а проверкой: в
  `stream.py::wifi()` цикл регистрирует и проверяет, пока все шесть не лягут.
  Мерено пробой `mask/diag.py`, она пишет на флеш и моста не создаёт.
- **Микрофон — внешний INMP441 на гребёнке** (22/23/24), дюпонтами, не паян.
  Встроенный на месте и остаётся запасным: переключение — константа
  `MIC_PINS` в `stream.py`, паяльник не нужен.
- **Пороги VAD сдвинуты**: `FLOOR_MIN` 15 → 60, у внешнего микрофона фон
  громче. Число на глаз, перемерю по живой речи — если увидишь в `heard/`
  пустые куски, это оно.
- Связь рвётся сама, до пятнадцати раз за полторы минуты, и это воздух:
  мерено с обоими микрофонами, разницы нет, пинг даёт джиттер до 120 мс до
  обеих коробок. `listen.py` это переживает, нарезка теперь тоже — раньше она
  заводилась заново на каждом обрыве и роняла порог на дно.
- 192.168.1.149 закреплён за маской в роутере по маске.
- Гейтвей я **не перезапускал**, конфига Гермеса не касался.

## 2 сентября, ночь — Старый бакет удалён, ссылки переписаны на `mara-vault`

Remotely Save перецелен на всех устройствах. `obsidian-vault` сутки не
принимал ни одной записи (`rclone lsl --max-age 24h` — ноль, самое свежее от
01:38 первого сентября, то есть момент переезда), значит подпорка своё
отработала.

Перед удалением бакет слит целиком на doctor:
`/srv/backup/r2-obsidian-vault-2026-09-02/`, 207 объектов, 113 МБ,
`rclone check --one-way` — 0 расхождений, 207 совпало. Слито **без фильтров**:
59 файлов из бакета в волте не существуют — копии репозиториев, вычищенные на
шаге 3 реструктуризации, две сироты селфтеста и `_probe2.txt`. Все они и так
были в списках исключений, но проверять по одному дольше, чем сохранить всё.

`rclone purge` прошёл с `rc=0` и снёс в том числе те 14 объектов, которые
первого сентября детерминированно отдавали 500 на `DeleteObject`: за сутки
Cloudflare их отпустил. Отдельный `scripts/r2-retry-orphans.sh` для этого уже
не понадобился — удалён вместе с `config/r2-orphans.txt`. Туда же
`scripts/r2-legacy-pull.sh`, `config/r2-legacy-pull.txt` и строка в crontab
doctor.

`REMOTE` по умолчанию в `stage0-selftest.sh` и `purge-from-vault.sh` переведён
на `mara-vault`. Второй важнее: он вычищает секрет из волта, git-истории,
зеркала и R2 — и, оставшись со старым дефолтом, удалял бы секрет из мёртвого
бакета, а в живом оставлял. `obsidian-vault` в `config/git-repos.txt` не
тронут: это репозиторий на GitHub, а не бакет.

Фильтры `config/r2-filters.txt` не менялись — значит `--resync` не нужен и
базовая линия bisync цела.

## 2 сентября, ночь — Маска не носимая, стрим стал настоящими 16 кГц

Снято допущение, что маска на голове: она висит на стене у телевизора, хозяин
ходит по комнате и лежит в кровати в семи-восьми метрах. Отсюда две поправки в
`mara/mask`: слышать надо с кровати (шёпот и крик включительно), а перебить её
голосом по одной громкости нельзя — микрофон и динамик на одной маске, громче в
микрофоне всегда она. `BARGE_K` ждёт живого динамика и замера.

Стрим с платы нёс 8 кГц, растянутые нулями до 16: буфер I2S — 32-битные слова,
левое и нулевое правое, и старая «таблица удвоения частоты» считала слова за
отсчёты. Теперь `RATE = 16000`, разбор каналов через `ustruct` на плате, на
проводе те же 4096 байт за 128 мс. Whisper получает полосу целиком вместо
зеркала низов в верхней половине.

Гипотеза про разрядность проверена и снята: 24 бита микрофона плата берёт, но
пол — комната, а не квантование. Шёпот с кровати по паспорту INMP441 ложится
на пол; решает минута шёпота под `listen.py --dump`, не оценка. Если не
оторвётся — второй микрофон у кровати.

## 2 сентября, ночь — Один набор плагинов на всех; ключи Copilot вычищены из истории

Плагины сведены к четырём: `remotely-save`, `copilot`, `omnisearch`, `calendar`.
Считалось по хранилищу, не по вкусу: 1503 заметки, ноль запросов Dataview и
Tasks, ноль чекбоксов с эмодзи Tasks, один excalidraw и один PDF, а вьюхи уже
на Bases. Smart Connections и Smart Lookup дублировали Copilot. Синк папки
конфига включён на всех устройствах; хозяин снял плагины руками на каждом.

Утечка. С синком конфига устройства заливают `.obsidian/` в бакет напрямую,
мимо фильтра doctor. Игнор RS закрыл `data.json`, но Copilot 3.3 при миграции
положил рядом `data-v3-credentials-backup-bc482a81.json` — копию с четырьмя
живыми ключами. В 03:07 он оказался в R2, в 03:15 через bisync — в git doctor'а.
Сам `copilot/data.json` в git лежал с 31 августа (легаси-забор). Remote у волта
нет, наружу это не уходило: R2 и бандлы на носителях. Сделано: файлы удалены
из R2 руками; `purge-from-vault.sh` переписал историю (75 коммитов), пересоздал
зеркало, снёс бандлы 1 сентября и собрал новые по 150 МБ. Скрипт по дороге
починен: маркер `already_ran` от прошлого прогона старше суток заставлял
filter-repo спрашивать «Y/N» в пустой stdin, вычистка вставала с EOFError.

Три слоя против повтора: игнор RS на устройствах префиксом `data` (пока на
хозяине), `.gitignore` волта (`plugins/copilot/data*`,
`plugins/remotely-save/data*`), фильтр R2 `plugins/*/data*.json` — за ним
надзорный resync 04:07–04:10, 2 м 52 с, rc=0.

Урок про «последний пишет»: телефон синкнулся последним и залил `app.json` из
двух байт, исключение поиска пропало. Настройки правятся на одном устройстве,
остальные принимают; возвращено с doctor.

Вход: `Главная.md` вместо стоковой «Добро пожаловать» (хозяин удалил, там было
только «удалите эту заметку»), `views/Сущности.base` группирует все сущности по
папке, `bookmarks.json` держит закладку на всех устройствах. `daily-notes`
переведён на `daily/`, чтобы Calendar открывал собранные кроном страницы;
`daily-page.py` к чужому файлу дописывает свой блок, тест на это есть. Родной
Obsidian Sync в ядре выключен.

На утро у хозяина: ротация четырёх ключей, укоротить строки игнора на всех
устройствах, Reload на каждом.

## 2 сентября, ночь — SOUL.md до Мары не дошёл: перезапуск шлюза не поможет, нужен /new или /model

Проверено по коду Гермеса (HEAD c30ac90) и по базе `~/.hermes/state.db`, не по
догадке. Системник собирается один раз на сессию и хранится в таблице
`system_prompts` по хэшу (`sessions.system_prompt_hash`; колонка
`sessions.system_prompt` пустая по замыслу, а не потому что промпта нет). На
каждой реплике он восстанавливается как есть, пока сходятся строки
Model/Provider/Platform/cwd. Пересобирается только на первой реплике новой
сессии, при `/model`, при сжатии контекста и при смене эпохи Bot Chat.
Перезапуска шлюза в этом списке нет: строка в базе переживает его, и Мара
просыпается с тем же промптом. Запись выше «перезапуск нужен, но не сделан»
снимается — он бесполезен.

У живой телеграм-сессии (`20260828_154102_a06b41b4`) промпт длиной 31887,
собран 29 августа в 11:36 при смене модели, и в нём нет ни «## Лицо», ни
«боится», ни «## Напоминания», ни ⏰: всё, что писалось в SOUL.md с тех пор,
она не видела ни разу.

Как донести: `/new` в телеграме (история сессии теряется) или `/model` —
`update_session_model` обнуляет `system_prompt_hash`, и на следующей реплике
промпт собирается заново, история остаётся; если на ту же модель Гермес
переключать откажется, туда и обратно. `session_reset.mode: none` — сама она
не обновится никогда. Ни того ни другого не делал: чат хозяина.

Попутно: каждая реплика этой сессии уносит около 136 тысяч входных токенов
(455 сообщений в истории). Это к архитектору, не к ночи.

## 2 сентября, ночь — Маска: сетка для центровки глаза, слушатель под launchd с дампом

`eye.grid()` и команда «сетка X Y» тем же файлом `~/mask/face`, что и
выражения: крест, засечки через 20 px, кольцо радужки, рамка видимой зоны
262x197; найденный сдвиг пишется в `eye.SHIFT`. На плате разбирается защитно,
кривая команда стрим не роняет — проверено в REPL и через файл. Подробности в
`mara/docs/mask-face.md`, коммит d44b049.

Слушатель на маке снова под launchd (домен `gui/501`: хозяин залогинен в
консоль), установленный plist временно несёт `--dump /tmp/ear.pcm` под
утренний замер шёпота и крика с кровати. Пики раз в секунду — в
`~/mask/listen.log`, сырой поток — `/tmp/ear.pcm` (~115 МБ/ч). После замера
plist вернуть из репо, шаги в `mask-bringup.md`. Флеш платы и `~/mask` на маке
вычищены от ночных проб; камеру, порог VAD, BARGE_K и разъём питания не трогал.

Дополнение той же ночи: сдвиг сетки ограничен кромкой панели (`eye.centre`).
Глаз, уехавший за экран, рисуется секундами на кадр, и пять кадров перехода
на каждом переподключении роняли стрим: 57 % времени в эфире против 78 % с
глазом на месте. Найдено собственным тестом «сетка 999 -999», залито, коммит
в mara следом за d44b049.

## 2 сентября, ночь — Мара получила «штырь в затылок»: сводка сущностей в SOUL.md

Серёга хотел, чтобы Мара знала волт, а не искала каждый раз. Инфраструктура
уже была (туннель, MCP, ingest), не хватало двух вещей: имён в промпте и живого
промпта у её телеграм-сессии.

Имена: `scripts/mara-brief.py` собирает из `entities/` блок «Что ты уже знаешь
о Серёге» — 52 карточки, 10,6 КБ, без модели, `sensitive: true` не попадает —
и врезает его между маркерами в `~/.hermes/SOUL.md` перед «Когда он изливает
душу». Копия в `_system/mara-brief.md`, крон `20 4 * * *` на doctor. Приёмка:
`hermes chat -q` про Atta-dipa и doctor — ответ по делу за 7 с, ноль вызовов
инструментов.

Живой промпт: запись выше права, рестарт gateway (сделан, безвреден) промпт
телеграм-сессии не обновляет. Сделано то, что делает `/model`: у сессии
`20260828_154102_a06b41b4` обнулены `system_prompt` и `system_prompt_hash`
(`state.db.bak-before-brief` рядом), история из 455 сообщений на месте, на
первой утренней реплике промпт соберётся из текущего SOUL.md — вместе с
«## Лицо» и напоминаниями, которых она тоже не видела. Скрипт делает тот же
`UPDATE` сам, когда блок изменился; дата из блока убрана, чтобы не рвать кэш
префикса каждую ночь впустую. Новый промпт будет ~48 КБ против 32 — это
+4 тыс. токенов на реплику к тем 136 тыс. истории, что уже помечены как вопрос
к архитектору.

Попутно: `install/stage0-doctor.sh` теперь кладёт в `.gitignore` волта те же
две строки про `plugins/*/data*`, что добавлены руками после утечки ключей.

## 2 сентября, утро — Питание маски закрыто: блок 12 В вместе с USB, по схеме и по замеру

Схема Maixduino 2832 нашлась на dl.sipeed.com: гнездо → диод → импульсный
EUP3458 → шина 5 В; USB через предохранитель и P-MOSFET, которым рулит LM321
(порог 6,6 В на гнезде). Регулятор не линейный, как думали, и оба источника
вместе держать можно. Хозяин промерил: USB — 5V 4,8, VIN 0; домашний блок
12 В/3 А — 5V 5,0, VIN 12, вместе с USB консоль отвечает, чип чуть тёплый.
Блок остаётся питанием маски, заказ девятивольтового снят. Гипотеза «обрывы
от просадки шины 4,8 В» снята замером: на блоке те же 4 обрыва в минуту.
Подробности в `mara/docs/mask-bringup.md`, раздел «Питание».

## 2 сентября, утро — Шёпот с кровати слышен: второй микрофон не нужен, теряют его только обрывы

Замер хозяина: шёпот с кровати по речевой полосе медиана 23 при тишине 5–8 и
пороге 15, крик оттуда же 563 без клиппинга. Нарезка режет шёпот, Whisper его
читает, «Мара, привет!» с кровати прошло как обращение, и она ответила. Что
теряет шёпот — обрывы связи с мостом: 22 за 4,5 минуты, каждый сбрасывает
набранный кусок. Это теперь главная задача маски. Дамп снят, plist на маке
возвращён к репозиторному, срез замера лежит в `~/mask/whisper_test_20260902.pcm`
на маке для подбора порогов без железа. Подробности — `mara/docs/mask-bringup.md`.

## 2 сентября, утро — Провайдер Мары: лесенка StreamLake → GMICloud → Alibaba вместо Baidu

Утром Baidu пятнадцать раз ответил 429 «rate-limited upstream», Гермес после
трёх повторов слал в телеграм «model server is not responding», а запасной
qwen на большом компьютере не ответил из-за проброса. По таблице OpenRouter
Baidu тринадцатый из восемнадцати по цене (1.69/3.38, кэш 0.14), StreamLake и
GMICloud fp8 — 1.04/2.08 при кэше 0.087. Хозяин решил: в пресете
`mara-baidu-fp8` на OpenRouter `order: StreamLake, GMICloud, Alibaba`,
`allow_fallbacks: false`, квант fp8. Лесенка у роутера, а не в
`fallback_providers`: роутер перебирает внутри одного запроса, Гермес — через
47 секунд. Пресет правит хозяин в своём аккаунте, config.yaml не трогается,
имя пресета остаётся. Записано в `mara/docs/HANDOVER.md`, «Инференс».

Дополнение: пресет переведён на лесенку самим агентом через API, по слову
хозяина «сам выстави». Версия 4 активна: `only`/`order` streamlake, gmicloud,
alibaba, fp8, без fallbacks; проверка через `@preset` — ответил StreamLake.
Грабли на будущее: у OpenRouter нет «обновить пресет», новая версия делается
запросом `POST /presets/{slug}/chat/completions`, и в неё уезжают все поля
запроса — версия 3 две минуты несла `max_tokens: 8`, снята версией 4. Бэкап
версии 2 в `~/.hermes/backups/` на маке.

## 2 сентября, день — Лесенка переставлена: GMICloud → Alibaba → StreamLake

Со StreamLake первым модель на настоящем контексте Мары (78 тысяч токенов,
инструменты) дважды за час вернула одни размышления без текста: в 10:44
четыре пустых ответа подряд, выкрутилась; в 11:18 шесть подряд, Гермес ушёл
на запасной qwen, а мак в тот момент не видел большой комп в сети
(`No route to host`), в чат упала заглушка «model server is not responding». За пять дней на Baidu
пустых ответов не было ни разу; на коротких пробах все три хоста отвечают с
текстом. Роутер пустой ответ ошибкой не считает и дальше по лесенке не идёт,
поэтому лечится только порядком. Хозяин решил: версия 5 пресета, GMICloud
первым, StreamLake последним. Наблюдать день. Проброс на большом компе проверен и
ни при чём: задача, скрипт и журнал в порядке, qwen отвечает. Мак теряет большой
комп в сети на секунды (три раза за день), оба на WiFi, кабель у большого не
воткнут — это решение хозяина, на большом компе ничего не трогали. Записано в `mara/docs/HANDOVER.md`, «Инференс».

## 2 сентября, день — Обрывы маски: причина в прошивке моста, дешёвое лечение с обеих сторон

Причина обрывов найдена, задача из «главной» переходит в «ждёт решения хозяина».
Плата стала писать в консоль, с чем рвётся: все обрывы — `EIO`, его отдаёт
драйвер моста K210↔ESP32, когда прошивка ESP32 ответила «отправлено ноль
байт» — её TCP-буфер (около 6 КБ, 180 мс звука) полон, WiFi встал дольше.
После такого send мост считает сокет закрытым (`ENOTCONN`), поэтому повтор
посылки не помогает, а нарезка на куски не влезает в 128 мс цикла (59 дыр за
три минуты против трёх). Оставлено дешёвое: мак не выбрасывает набранный до
обрыва кусок, а отдаёт Whisper (`flush()`); плата на обрыве живой связи идёт
назад сразу, без двухсекундной паузы; в консоль пишутся «обрыв через N с»,
«связь за N мс», «не успели: дыра N мс». После правки: 0 обрывов за 4 и за 9
минут (было 5 за 200 с). Настоящее лечение — не TCP через этот мост: UDP или
прошивка моста, это смена конструкции, решает хозяин. Подробно —
`docs/mask-bringup.md` в mara, «Причина самих обрывов нашлась 2 сентября».

## 2 сентября, день — Звук маски переведён на UDP, плату сторожит WDT

Хозяин сказал «да» на UDP. Намерено перед переходом: мост ESP32 берёт
посылку только до 1460 байт (2000 и 4000 — EIO, а «прошедшие» 4000 приходили
обрезками 73–1119 байт), буфер 4200 байт уходит тремя посылками по 1400 за
35–44 мс. Три минуты подряд чисто: 4200 посылок, ни одной ошибки, все 175
ответов мака дошли; TCP в соседние минуты рвался десять раз за две минуты.
Выложено на мак и плату. Дальше два сюрприза, оба закрыты. Плата на седьмой
минуте повисла в C намертво (сорок Ctrl-C впустую, помог только сброс по
DTR) — поставлен аппаратный сторож K210, кормится раз в буфер, сброс через
82 с без кормёжки (мерено; сторож округляет к степени двойки). После серии
перезагрузок платы мак полчаса писал шум до упора шкалы: хвост недобранного
кадра переживал перезагрузку и клеился к новому потоку со сдвигом на байт —
теперь хвост выбрасывается на новом адресе, нечётные посылки выбрасываются
целиком. Проверено после: смена лица — пять дыр по 10–28 мс на смену, вне
смены дыр нет; приёмник выключен на 30 с — плата этого не замечает, сеть не
трогает, вернувшийся приёмник видит её первой посылкой. Цена UDP: потерянная
посылка — 44 мс тишины; вис — полторы минуты без слуха (82 с до сброса плюс
перезагрузка), причина виса не найдена, сторож — сеть, не лекарство. Смотреть:
`listen.log` («маска замолчала», следом «маска подключилась» без перезагрузки
руками = сработал сторож; «кривая посылка», «посылок не было N с»), консоль платы
(«посылка не ушла», «сеть заново»). Подробно — `docs/mask-bringup.md` в
mara, «UDP вместо TCP». Хозяину в обмен ничего не нужно.

Поправка 13:25 (сессия маски): коммит 3e79815 случайно прихватил четыре
чужих файла в работе (`install/mara-context/*`, `scripts/call_project.py`,
`scripts/contextd.py`) — в следующем коммите они возвращены к b9f95d6,
рабочая копия не тронута, коммитить их — тому, кто их правит.

## 2 сентября, 14:10 — SD-карта в маске: MaixPy грузится с карты

Хозяин вставил microSD 16 ГБ (FAT32, MBR). Плата её видит как `/sd`, но с
картой в слоте прошивка грузит `/sd/main.py`, а не `/flash/main.py`, и сама
пишет на карту заводскую заставку — после перезагрузки маска молчала, сеть не
поднималась. На карте теперь переходник `mask/main_sd.py` (chdir в /flash,
exec настоящего main.py), проверено сбросом: стрим вернулся за 5 с.
Хозяин поправил порядок: калибровку глаза и приклейку к маске — в самый
конец; сначала рабочий прототип на столе (камера, диалог, автономный запуск
без мака). Хозяину в обмен ничего не нужно.
